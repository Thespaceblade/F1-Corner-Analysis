(() => {
var exports = {};
exports.id = 931;
exports.ids = [931];
exports.modules = {

/***/ 18038:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react");

/***/ }),

/***/ 98704:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react-dom/server-rendering-stub");

/***/ }),

/***/ 97897:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react-server-dom-webpack/client");

/***/ }),

/***/ 56786:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/compiled/react/jsx-runtime");

/***/ }),

/***/ 5868:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/app-render/app-render");

/***/ }),

/***/ 41844:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/app-render/get-segment-param");

/***/ }),

/***/ 96624:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/future/helpers/interception-routes");

/***/ }),

/***/ 75281:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/server/future/route-modules/route-module");

/***/ }),

/***/ 57085:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/app-router-context");

/***/ }),

/***/ 1830:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/get-img-props");

/***/ }),

/***/ 20199:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/hash");

/***/ }),

/***/ 66864:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/head");

/***/ }),

/***/ 39569:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/hooks-client-context");

/***/ }),

/***/ 52210:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/image-config");

/***/ }),

/***/ 35359:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/image-config-context");

/***/ }),

/***/ 30893:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/add-path-prefix");

/***/ }),

/***/ 17887:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/handle-smooth-scroll");

/***/ }),

/***/ 98735:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/is-bot");

/***/ }),

/***/ 68231:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/parse-path");

/***/ }),

/***/ 53750:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/router/utils/remove-trailing-slash");

/***/ }),

/***/ 79618:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/server-inserted-html");

/***/ }),

/***/ 98658:
/***/ ((module) => {

"use strict";
module.exports = require("next/dist/shared/lib/utils/warn-once");

/***/ }),

/***/ 25934:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   GlobalError: () => (/* reexport safe */ next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_1__.GlobalError),
/* harmony export */   __next_app__: () => (/* binding */ __next_app__),
/* harmony export */   originalPathname: () => (/* binding */ originalPathname),
/* harmony export */   pages: () => (/* binding */ pages),
/* harmony export */   routeModule: () => (/* binding */ routeModule),
/* harmony export */   tree: () => (/* binding */ tree)
/* harmony export */ });
/* harmony import */ var next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(31240);
/* harmony import */ var next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(49506);
/* harmony import */ var next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(next_dist_client_components_error_boundary__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(95394);
/* harmony import */ var next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_2__);
/* harmony reexport (unknown) */ var __WEBPACK_REEXPORT_OBJECT__ = {};
/* harmony reexport (unknown) */ for(const __WEBPACK_IMPORT_KEY__ in next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_2__) if(["default","tree","pages","GlobalError","originalPathname","__next_app__","routeModule"].indexOf(__WEBPACK_IMPORT_KEY__) < 0) __WEBPACK_REEXPORT_OBJECT__[__WEBPACK_IMPORT_KEY__] = () => next_dist_server_app_render_entry_base__WEBPACK_IMPORTED_MODULE_2__[__WEBPACK_IMPORT_KEY__]
/* harmony reexport (unknown) */ __webpack_require__.d(__webpack_exports__, __WEBPACK_REEXPORT_OBJECT__);

    

    const tree = {
        children: [
        '',
        {
        children: ['__PAGE__', {}, {
          page: [() => Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 68810)), "/Users/jasoncharwin/Personal Code Projects/F1-Corner-Analysis/app/page.tsx"],
          
        }]
      },
        {
          'layout': [() => Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 75200)), "/Users/jasoncharwin/Personal Code Projects/F1-Corner-Analysis/app/layout.tsx"],
          
        }
      ]
      }.children;
    const pages = ["/Users/jasoncharwin/Personal Code Projects/F1-Corner-Analysis/app/page.tsx"];

    

    const originalPathname = "/page"
    const __next_app__ = {
      require: __webpack_require__,
      // all modules are in the entry chunk, so we never actually need to load chunks in webpack
      loadChunk: () => Promise.resolve()
    }

    

    // Create and export the route module that will be consumed.
    const options = {"definition":{"kind":"APP_PAGE","page":"/page","pathname":"/","bundlePath":"app/page","filename":"","appPaths":[]}}
    const routeModule = new (next_dist_server_future_route_modules_app_page_module__WEBPACK_IMPORTED_MODULE_0___default())({
      ...options,
      userland: {
        loaderTree: tree,
      },
    })
  

/***/ }),

/***/ 14287:
/***/ (() => {



/***/ }),

/***/ 30963:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

Promise.resolve(/* import() eager */).then(__webpack_require__.bind(__webpack_require__, 84900))

/***/ }),

/***/ 24270:
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

Promise.resolve(/* import() eager */).then(__webpack_require__.t.bind(__webpack_require__, 4249, 23));
Promise.resolve(/* import() eager */).then(__webpack_require__.t.bind(__webpack_require__, 94564, 23));
Promise.resolve(/* import() eager */).then(__webpack_require__.t.bind(__webpack_require__, 50885, 23));
Promise.resolve(/* import() eager */).then(__webpack_require__.t.bind(__webpack_require__, 80772, 23));
Promise.resolve(/* import() eager */).then(__webpack_require__.t.bind(__webpack_require__, 68262, 23))

/***/ }),

/***/ 84900:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  "default": () => (/* binding */ ClientPage)
});

// EXTERNAL MODULE: external "next/dist/compiled/react/jsx-runtime"
var jsx_runtime_ = __webpack_require__(56786);
// EXTERNAL MODULE: external "next/dist/compiled/react"
var react_ = __webpack_require__(18038);
var react_default = /*#__PURE__*/__webpack_require__.n(react_);
// EXTERNAL MODULE: ./node_modules/next/image.js
var next_image = __webpack_require__(48421);
var image_default = /*#__PURE__*/__webpack_require__.n(next_image);
;// CONCATENATED MODULE: ./lib/teamData.ts
const f1Teams = [
    {
        id: "red-bull",
        name: "Oracle Red Bull Racing",
        shortName: "Red Bull",
        color: "#000B8D",
        drivers: [
            {
                code: "VER",
                name: "Max Verstappen",
                number: 1
            },
            {
                code: "TSU",
                name: "Yuki Tsunoda",
                number: 22
            }
        ]
    },
    {
        id: "mclaren",
        name: "McLaren Formula 1 Team",
        shortName: "McLaren",
        color: "#FF8000",
        drivers: [
            {
                code: "NOR",
                name: "Lando Norris",
                number: 4
            },
            {
                code: "PIA",
                name: "Oscar Piastri",
                number: 81
            }
        ]
    },
    {
        id: "mercedes",
        name: "Mercedes-AMG PETRONAS F1 Team",
        shortName: "Mercedes",
        color: "#00A19B",
        drivers: [
            {
                code: "RUS",
                name: "George Russell",
                number: 63
            },
            {
                code: "ANT",
                name: "Kimi Antonelli",
                number: 12
            }
        ]
    },
    {
        id: "ferrari",
        name: "Scuderia Ferrari",
        shortName: "Ferrari",
        color: "#ED1131",
        drivers: [
            {
                code: "LEC",
                name: "Charles Leclerc",
                number: 16
            },
            {
                code: "HAM",
                name: "Lewis Hamilton",
                number: 44
            }
        ]
    },
    {
        id: "aston-martin",
        name: "Aston Martin Aramco Formula One Team",
        shortName: "Aston Martin",
        color: "#358C75",
        drivers: [
            {
                code: "ALO",
                name: "Fernando Alonso",
                number: 14
            },
            {
                code: "STR",
                name: "Lance Stroll",
                number: 18
            }
        ]
    },
    {
        id: "alpine",
        name: "BWT Alpine F1 Team",
        shortName: "Alpine",
        color: "#2293D1",
        drivers: [
            {
                code: "COL",
                name: "Franco Colapinto",
                number: 43
            },
            {
                code: "GAS",
                name: "Pierre Gasly",
                number: 10
            }
        ]
    },
    {
        id: "williams",
        name: "Williams Racing",
        shortName: "Williams",
        color: "#37BEDD",
        drivers: [
            {
                code: "ALB",
                name: "Alexander Albon",
                number: 23
            },
            {
                code: "SAI",
                name: "Carlos Sainz",
                number: 55
            }
        ]
    },
    {
        id: "visa-rb",
        name: "Visa Cash App RB Formula One Team",
        shortName: "RB",
        color: "#5E8FAA",
        drivers: [
            {
                code: "LAW",
                name: "Liam Lawson",
                number: 30
            },
            {
                code: "HAD",
                name: "Isack Hadjar",
                number: 6
            }
        ]
    },
    {
        id: "stake",
        name: "Stake F1 Team Kick Sauber",
        shortName: "Stake",
        color: "#52E252",
        drivers: [
            {
                code: "HUL",
                name: "Nico Hulkenburg",
                number: 27
            },
            {
                code: "BOR",
                name: "Gabriel Bortoleto",
                number: 5
            }
        ]
    },
    {
        id: "haas",
        name: "MoneyGram Haas F1 Team",
        shortName: "Haas",
        color: "#B6BABD",
        drivers: [
            {
                code: "OCO",
                name: "Esteban Ocon",
                number: 31
            },
            {
                code: "BEA",
                name: "Oliver Bearman",
                number: 87
            }
        ]
    }
];
const driverColorMap = f1Teams.reduce((acc, team)=>{
    team.drivers.forEach((driver)=>{
        acc[driver.code.toUpperCase()] = team.color;
    });
    return acc;
}, {});

;// CONCATENATED MODULE: ./components/CustomSelect.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 

function CustomSelect({ options, value, onChange, placeholder = "Select...", className = "", minWidth = "auto" }) {
    const [isOpen, setIsOpen] = (0,react_.useState)(false);
    const [hoveredIndex, setHoveredIndex] = (0,react_.useState)(null);
    const containerRef = (0,react_.useRef)(null);
    const listRef = (0,react_.useRef)(null);
    const selectedOption = options.find((opt)=>opt.value === value);
    // Close dropdown when clicking outside
    (0,react_.useEffect)(()=>{
        const handleClickOutside = (event)=>{
            if (containerRef.current && !containerRef.current.contains(event.target)) {
                setIsOpen(false);
            }
        };
        if (isOpen) {
            document.addEventListener("mousedown", handleClickOutside);
            return ()=>document.removeEventListener("mousedown", handleClickOutside);
        }
    }, [
        isOpen
    ]);
    // Handle keyboard navigation
    (0,react_.useEffect)(()=>{
        if (!isOpen) return;
        const handleKeyDown = (e)=>{
            if (e.key === "Escape") {
                setIsOpen(false);
                return;
            }
            if (e.key === "ArrowDown" || e.key === "ArrowUp") {
                e.preventDefault();
                const currentIndex = options.findIndex((opt)=>opt.value === value);
                let newIndex = currentIndex;
                if (e.key === "ArrowDown") {
                    newIndex = currentIndex < options.length - 1 ? currentIndex + 1 : 0;
                } else {
                    newIndex = currentIndex > 0 ? currentIndex - 1 : options.length - 1;
                }
                onChange(options[newIndex].value);
                setHoveredIndex(newIndex);
                // Scroll into view
                if (listRef.current) {
                    const items = listRef.current.children;
                    if (items[newIndex]) {
                        items[newIndex].scrollIntoView({
                            block: "nearest",
                            behavior: "smooth"
                        });
                    }
                }
            }
            if (e.key === "Enter" && hoveredIndex !== null) {
                onChange(options[hoveredIndex].value);
                setIsOpen(false);
            }
        };
        document.addEventListener("keydown", handleKeyDown);
        return ()=>document.removeEventListener("keydown", handleKeyDown);
    }, [
        isOpen,
        value,
        options,
        hoveredIndex,
        onChange
    ]);
    const handleOptionClick = (optionValue)=>{
        onChange(optionValue);
        setIsOpen(false);
        setHoveredIndex(null);
    };
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
        ref: containerRef,
        className: `relative ${className}`,
        style: {
            minWidth
        },
        children: [
            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("button", {
                type: "button",
                onClick: ()=>setIsOpen(!isOpen),
                className: `
          input-slim w-full flex items-center justify-between gap-2
          transition-all duration-200 ease-out
          ${isOpen ? "border-accent/60 bg-gray-800/50 shadow-[0_0_0_3px_rgba(124,199,255,0.1)]" : "border-gray-600/60 hover:border-accent/40 hover:bg-gray-800/30"}
          focus:outline-none focus:ring-2 focus:ring-accent/30 focus:border-accent/60
        `,
                "aria-expanded": isOpen,
                "aria-haspopup": "listbox",
                children: [
                    /*#__PURE__*/ jsx_runtime_.jsx("span", {
                        className: `truncate ${selectedOption ? "text-white" : "text-gray-400"}`,
                        children: selectedOption ? selectedOption.label : placeholder
                    }),
                    /*#__PURE__*/ jsx_runtime_.jsx("svg", {
                        className: `w-4 h-4 text-gray-400 transition-transform duration-200 ${isOpen ? "rotate-180 text-accent" : ""}`,
                        fill: "none",
                        stroke: "currentColor",
                        viewBox: "0 0 24 24",
                        children: /*#__PURE__*/ jsx_runtime_.jsx("path", {
                            strokeLinecap: "round",
                            strokeLinejoin: "round",
                            strokeWidth: 2,
                            d: "M19 9l-7 7-7-7"
                        })
                    })
                ]
            }),
            isOpen && /*#__PURE__*/ jsx_runtime_.jsx("ul", {
                ref: listRef,
                role: "listbox",
                className: `
            absolute z-50 w-full mt-1.5
            bg-gray-900/98 backdrop-blur-md
            border border-gray-700/70
            rounded-lg shadow-2xl
            max-h-64 overflow-auto
            py-1.5
            animate-in fade-in slide-in-from-top-2 duration-200
            scrollbar-thin scrollbar-thumb-gray-600 scrollbar-track-transparent
          `,
                style: {
                    boxShadow: "0 10px 40px rgba(0, 0, 0, 0.5), 0 0 0 1px rgba(255, 255, 255, 0.05) inset"
                },
                children: options.map((option, index)=>{
                    const isSelected = option.value === value;
                    const isHovered = hoveredIndex === index;
                    return /*#__PURE__*/ jsx_runtime_.jsx("li", {
                        role: "option",
                        "aria-selected": isSelected,
                        onClick: ()=>handleOptionClick(option.value),
                        onMouseEnter: ()=>setHoveredIndex(index),
                        onMouseLeave: ()=>setHoveredIndex(null),
                        className: `
                  px-3 py-2 mx-1.5 rounded-md cursor-pointer
                  transition-all duration-150 ease-out
                  ${isSelected ? "bg-accent/20 text-accent font-medium" : isHovered ? "bg-gray-700/60 text-white" : "text-gray-300"}
                  ${isHovered || isSelected ? "transform scale-[1.02]" : ""}
                `,
                        children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                            className: "flex items-center justify-between",
                            children: [
                                /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                    className: "truncate",
                                    children: option.label
                                }),
                                isSelected && /*#__PURE__*/ jsx_runtime_.jsx("svg", {
                                    className: "w-4 h-4 text-accent ml-2 flex-shrink-0",
                                    fill: "currentColor",
                                    viewBox: "0 0 20 20",
                                    children: /*#__PURE__*/ jsx_runtime_.jsx("path", {
                                        fillRule: "evenodd",
                                        d: "M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z",
                                        clipRule: "evenodd"
                                    })
                                })
                            ]
                        })
                    }, option.value);
                })
            })
        ]
    });
}

;// CONCATENATED MODULE: ./components/Toolbar.tsx
/* __next_internal_client_entry_do_not_use__ sessionOptions,default auto */ 



const sessionOptions = [
    {
        label: "Qualifying",
        value: "Q"
    },
    {
        label: "Race",
        value: "R"
    },
    {
        label: "Sprint Qualifying",
        value: "SQ"
    },
    {
        label: "Sprint",
        value: "S"
    }
];
// Tracks that have sprint weekends (Sprint Qualifying and Sprint sessions)
const SPRINT_WEEKEND_TRACKS = new Set([
    "china",
    "miami",
    "belgium",
    "united-states",
    "brazil",
    "qatar"
]);
function Toolbar({ tracks, selectedTrack, onTrackChangeAction, years, selectedYear, onYearChangeAction, selectedDrivers, onDriversChangeAction, selectedSession, onSessionChangeAction, availableSessions = [] }) {
    const [openTeamId, setOpenTeamId] = (0,react_.useState)(null);
    const closeTimer = (0,react_.useRef)(null);
    const currentSelection = (0,react_.useMemo)(()=>new Set(selectedDrivers), [
        selectedDrivers
    ]);
    const cancelClose = ()=>{
        if (closeTimer.current) {
            clearTimeout(closeTimer.current);
            closeTimer.current = null;
        }
    };
    const scheduleClose = (teamId)=>{
        cancelClose();
        closeTimer.current = setTimeout(()=>{
            setOpenTeamId((prev)=>prev === teamId ? null : prev);
            closeTimer.current = null;
        }, 120);
    };
    const openTeam = (teamId)=>{
        cancelClose();
        setOpenTeamId(teamId);
    };
    (0,react_.useEffect)(()=>()=>cancelClose(), []);
    const toggleDriver = (driverCode)=>{
        onDriversChangeAction(currentSelection.has(driverCode) ? selectedDrivers.filter((code)=>code !== driverCode) : [
            ...selectedDrivers,
            driverCode
        ]);
    };
    const toggleTeam = (teamDriverCodes)=>{
        const allSelected = teamDriverCodes.every((code)=>currentSelection.has(code));
        if (allSelected) {
            onDriversChangeAction(selectedDrivers.filter((code)=>!teamDriverCodes.includes(code)));
        } else {
            const merged = new Set(selectedDrivers);
            teamDriverCodes.forEach((code)=>merged.add(code));
            onDriversChangeAction(Array.from(merged));
        }
    };
    const yearOptions = (0,react_.useMemo)(()=>years.map((y)=>({
                value: y,
                label: String(y)
            })), [
        years
    ]);
    const trackOptions = (0,react_.useMemo)(()=>[
            {
                value: "",
                label: "Select Track"
            },
            ...tracks.map((track)=>({
                    value: track.id,
                    label: track.name
                }))
        ], [
        tracks
    ]);
    const sessionOptionsList = (0,react_.useMemo)(()=>{
        // Show all session options by default
        if (!selectedTrack || availableSessions.length === 0) {
            return sessionOptions.map((option)=>({
                    value: option.value,
                    label: option.label
                }));
        }
        // Check if this is a known sprint weekend track
        const isSprintWeekend = SPRINT_WEEKEND_TRACKS.has(selectedTrack);
        // When we have confirmed session data for a track:
        const hasQualifyingOrRace = availableSessions.some((s)=>s === "Q" || s === "R");
        const hasSprint = availableSessions.some((s)=>s === "SQ" || s === "S");
        if (isSprintWeekend) {
            // Known sprint weekend - always show sprint options even if data not fetched yet
            return sessionOptions.map((option)=>({
                    value: option.value,
                    label: option.label
                }));
        }
        if (hasQualifyingOrRace && !hasSprint) {
            // We have race data but no sprint data - this race didn't have sprints
            return sessionOptions.filter((option)=>option.value !== "SQ" && option.value !== "S").map((option)=>({
                    value: option.value,
                    label: option.label
                }));
        }
        // Show all options (either we have sprint data, or data is incomplete)
        return sessionOptions.map((option)=>({
                value: option.value,
                label: option.label
            }));
    }, [
        availableSessions,
        selectedTrack
    ]);
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
        className: "panel p-3 flex flex-col sm:flex-row sm:items-center gap-3",
        children: [
            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                className: "flex items-center gap-3",
                children: [
                    /*#__PURE__*/ jsx_runtime_.jsx(CustomSelect, {
                        options: yearOptions,
                        value: selectedYear,
                        onChange: (val)=>onYearChangeAction(Number(val)),
                        className: "w-[100px]",
                        minWidth: "100px"
                    }),
                    /*#__PURE__*/ jsx_runtime_.jsx(CustomSelect, {
                        options: trackOptions,
                        value: selectedTrack,
                        onChange: (val)=>onTrackChangeAction(String(val)),
                        placeholder: "Select Track",
                        className: "flex-1",
                        minWidth: "240px"
                    }),
                    /*#__PURE__*/ jsx_runtime_.jsx(CustomSelect, {
                        options: sessionOptionsList,
                        value: selectedSession,
                        onChange: (val)=>onSessionChangeAction(String(val)),
                        className: "flex-1",
                        minWidth: "180px"
                    })
                ]
            }),
            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "flex-1 flex items-center justify-end gap-2 flex-wrap",
                children: f1Teams.map((team)=>{
                    const teamDriverCodes = team.drivers.map((driver)=>driver.code);
                    const allSelected = teamDriverCodes.every((code)=>currentSelection.has(code));
                    const anySelected = allSelected || teamDriverCodes.some((code)=>currentSelection.has(code));
                    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        className: "relative",
                        "data-team-wrapper": "true",
                        onMouseEnter: ()=>openTeam(team.id),
                        onMouseLeave: ()=>scheduleClose(team.id),
                        children: [
                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("button", {
                                type: "button",
                                className: "chip relative flex h-[2.75rem] w-[2.75rem] items-center justify-center p-0",
                                style: {
                                    backgroundColor: team.color
                                },
                                title: team.name,
                                "aria-pressed": allSelected,
                                "aria-expanded": openTeamId === team.id,
                                "aria-controls": `team-menu-${team.id}`,
                                onClick: ()=>{
                                    openTeam(team.id);
                                    toggleTeam(teamDriverCodes);
                                },
                                onFocus: ()=>openTeam(team.id),
                                onBlur: (event)=>{
                                    const next = event.relatedTarget;
                                    const wrapper = event.currentTarget.parentElement;
                                    if (next && wrapper && wrapper.contains(next)) return;
                                    scheduleClose(team.id);
                                },
                                children: [
                                    /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                        className: `absolute inset-0 rounded-full transition-all duration-150 ${allSelected ? "ring-2 ring-white/90" : anySelected ? "ring-2 ring-white/40" : "ring-0"}`,
                                        "aria-hidden": "true"
                                    }),
                                    /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                        className: "relative z-10 flex h-full w-full items-center justify-center",
                                        children: [
                                            "red-bull",
                                            "mercedes",
                                            "ferrari",
                                            "mclaren",
                                            "alpine",
                                            "williams",
                                            "aston-martin",
                                            "visa-rb",
                                            "stake",
                                            "haas"
                                        ].includes(team.id) ? /*#__PURE__*/ jsx_runtime_.jsx("img", {
                                            src: `/team-logos/${team.id}.png`,
                                            alt: `${team.shortName} logo`,
                                            className: `h-full w-full object-contain ${[
                                                "aston-martin",
                                                "visa-rb",
                                                "stake"
                                            ].includes(team.id) ? "scale-110" : ""}`
                                        }) : /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                            className: "text-sm font-semibold",
                                            children: team.shortName.substring(0, 2)
                                        })
                                    })
                                ]
                            }),
                            openTeamId === team.id && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                id: `team-menu-${team.id}`,
                                role: "menu",
                                className: "absolute top-full left-1/2 z-50 min-w-[140px] -translate-x-1/2 translate-y-2 rounded-lg border border-gray-700/70 bg-gray-900/95 p-2 shadow-2xl backdrop-blur",
                                onMouseEnter: ()=>openTeam(team.id),
                                onMouseLeave: ()=>scheduleClose(team.id),
                                children: team.drivers.map((driver)=>{
                                    const isSelected = currentSelection.has(driver.code);
                                    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("button", {
                                        type: "button",
                                        role: "menuitemcheckbox",
                                        "aria-checked": isSelected,
                                        onClick: ()=>toggleDriver(driver.code),
                                        className: `w-full rounded px-2 py-1 text-left transition-colors ${isSelected ? "bg-gray-700 text-accent" : "text-gray-200 hover:bg-gray-700"}`,
                                        onFocus: ()=>openTeam(team.id),
                                        onBlur: (event)=>{
                                            const next = event.relatedTarget;
                                            const parent = event.currentTarget.closest("[data-team-wrapper]");
                                            if (next && parent && parent.contains(next)) return;
                                            scheduleClose(team.id);
                                        },
                                        children: [
                                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("span", {
                                                className: "inline-flex w-16 items-center gap-2 font-mono text-sm",
                                                children: [
                                                    /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                        className: `inline-flex h-2.5 w-2.5 items-center justify-center rounded-full border border-gray-500 ${isSelected ? "border-accent bg-accent" : "bg-transparent"}`,
                                                        "aria-hidden": "true"
                                                    }),
                                                    driver.code
                                                ]
                                            }),
                                            /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                className: "text-sm text-gray-400",
                                                children: driver.number
                                            })
                                        ]
                                    }, driver.code);
                                })
                            })
                        ]
                    }, team.id);
                })
            })
        ]
    });
}

;// CONCATENATED MODULE: ./lib/trackSvgLoader.ts
// Utility to load inline SVG content for track maps.
// Uses webpack's ?raw suffix to import the file contents as a string at build time.
const svgLoaders = {
    "abudhabi.svg": ()=>__webpack_require__.e(/* import() */ 894).then(__webpack_require__.t.bind(__webpack_require__, 11894, 17)),
    "australia.svg": ()=>__webpack_require__.e(/* import() */ 276).then(__webpack_require__.t.bind(__webpack_require__, 54698, 17)),
    "austria.svg": ()=>__webpack_require__.e(/* import() */ 743).then(__webpack_require__.t.bind(__webpack_require__, 61743, 17)),
    "azerbaijan.svg": ()=>__webpack_require__.e(/* import() */ 999).then(__webpack_require__.t.bind(__webpack_require__, 75999, 17)),
    "bahrain.svg": ()=>__webpack_require__.e(/* import() */ 307).then(__webpack_require__.t.bind(__webpack_require__, 19307, 17)),
    "brazil.svg": ()=>__webpack_require__.e(/* import() */ 589).then(__webpack_require__.t.bind(__webpack_require__, 32589, 17)),
    "canada.svg": ()=>__webpack_require__.e(/* import() */ 527).then(__webpack_require__.t.bind(__webpack_require__, 43527, 17)),
    "china.svg": ()=>__webpack_require__.e(/* import() */ 332).then(__webpack_require__.t.bind(__webpack_require__, 30332, 17)),
    "hungary.svg": ()=>__webpack_require__.e(/* import() */ 564).then(__webpack_require__.t.bind(__webpack_require__, 53564, 17)),
    "imola.svg": ()=>__webpack_require__.e(/* import() */ 963).then(__webpack_require__.t.bind(__webpack_require__, 10963, 17)),
    "japan.svg": ()=>__webpack_require__.e(/* import() */ 77).then(__webpack_require__.t.bind(__webpack_require__, 63077, 17)),
    "las_vegas.svg": ()=>__webpack_require__.e(/* import() */ 691).then(__webpack_require__.t.bind(__webpack_require__, 98691, 17)),
    "mexico.svg": ()=>__webpack_require__.e(/* import() */ 619).then(__webpack_require__.t.bind(__webpack_require__, 52619, 17)),
    "miami.svg": ()=>__webpack_require__.e(/* import() */ 493).then(__webpack_require__.t.bind(__webpack_require__, 35493, 17)),
    "monaco.svg": ()=>__webpack_require__.e(/* import() */ 413).then(__webpack_require__.t.bind(__webpack_require__, 13413, 17)),
    "monza.svg": ()=>__webpack_require__.e(/* import() */ 838).then(__webpack_require__.t.bind(__webpack_require__, 58838, 17)),
    "netherlands.svg": ()=>__webpack_require__.e(/* import() */ 932).then(__webpack_require__.t.bind(__webpack_require__, 48932, 17)),
    "qatar.svg": ()=>__webpack_require__.e(/* import() */ 698).then(__webpack_require__.t.bind(__webpack_require__, 49698, 17)),
    "saudi_arabia.svg": ()=>__webpack_require__.e(/* import() */ 328).then(__webpack_require__.t.bind(__webpack_require__, 73328, 17)),
    "silverstone.svg": ()=>__webpack_require__.e(/* import() */ 75).then(__webpack_require__.t.bind(__webpack_require__, 37075, 17)),
    "singapore.svg": ()=>__webpack_require__.e(/* import() */ 258).then(__webpack_require__.t.bind(__webpack_require__, 11258, 17)),
    "spa.svg": ()=>__webpack_require__.e(/* import() */ 705).then(__webpack_require__.t.bind(__webpack_require__, 15705, 17)),
    "spain.svg": ()=>__webpack_require__.e(/* import() */ 115).then(__webpack_require__.t.bind(__webpack_require__, 96115, 17)),
    "usa.svg": ()=>__webpack_require__.e(/* import() */ 540).then(__webpack_require__.t.bind(__webpack_require__, 90540, 17))
};
async function loadTrackSvg(svgFile) {
    const loader = svgLoaders[svgFile];
    if (!loader) {
        throw new Error(`Unsupported track SVG: ${svgFile}`);
    }
    const mod = await loader();
    if (typeof mod === "string") {
        return mod;
    }
    if (mod && typeof mod === "object" && typeof mod.default === "string") {
        return mod.default;
    }
    throw new Error(`Failed to resolve SVG contents for ${svgFile}`);
}

;// CONCATENATED MODULE: ./components/TrackPanel.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 


function TrackPanel({ svgFile, corners }) {
    const [svgContent, setSvgContent] = (0,react_.useState)(null);
    const [viewBox, setViewBox] = (0,react_.useState)(null);
    const [error, setError] = (0,react_.useState)(null);
    const [isReady, setIsReady] = (0,react_.useState)(false);
    const reqIdRef = react_default().useRef(0);
    (0,react_.useEffect)(()=>{
        if (!svgFile) return;
        // clear previous content immediately to avoid flashing the wrong track
        setSvgContent(null);
        setViewBox(null);
        setError(null);
        setIsReady(false);
        const localReqId = reqIdRef.current + 1;
        reqIdRef.current = localReqId;
        loadTrackSvg(svgFile).then((text)=>{
            // ignore if this is not the latest request
            if (localReqId !== reqIdRef.current) return;
            // try extract viewBox
            const vbMatch = text.match(/viewBox\s*=\s*"([^"]+)"/) || text.match(/viewBox\s*=\s*'([^']+)'/);
            if (vbMatch && vbMatch[1]) {
                const parts = vbMatch[1].trim().split(/\s+/).map(Number);
                if (parts.length === 4 && parts.every((p)=>!Number.isNaN(p))) {
                    // only set if still latest
                    if (localReqId === reqIdRef.current) {
                        setViewBox({
                            minX: parts[0],
                            minY: parts[1],
                            w: parts[2],
                            h: parts[3]
                        });
                    }
                }
            }
            if (localReqId === reqIdRef.current) {
                const prepared = text.replace(/<svg([\s\S]*?)>/i, (_match, attrs)=>{
                    let adjusted = attrs;
                    if (/width=/i.test(adjusted)) {
                        adjusted = adjusted.replace(/width\s*=\s*(["']).*?\1/i, 'width="100%"');
                    } else {
                        adjusted += ' width="100%"';
                    }
                    if (/height=/i.test(adjusted)) {
                        adjusted = adjusted.replace(/height\s*=\s*(["']).*?\1/i, 'height="100%"');
                    } else {
                        adjusted += ' height="100%"';
                    }
                    if (/preserveAspectRatio=/i.test(adjusted)) {
                        adjusted = adjusted.replace(/preserveAspectRatio\s*=\s*(["']).*?\1/i, 'preserveAspectRatio="xMidYMid meet"');
                    } else {
                        adjusted += ' preserveAspectRatio="xMidYMid meet"';
                    }
                    return `<svg${adjusted}>`;
                });
                setSvgContent(prepared);
            }
        }).catch((err)=>{
            // ignore if superseded
            if (localReqId !== reqIdRef.current) return;
            console.error("Failed to load SVG", svgFile, err);
            setError(`Failed to load ${svgFile}: ${err.message}`);
            setSvgContent(null);
            setIsReady(true);
        });
    }, [
        svgFile
    ]);
    // Once the raw SVG is loaded we can flip the ready flag
    (0,react_.useEffect)(()=>{
        if (!svgContent) return;
        setIsReady(true);
    }, [
        svgContent
    ]);
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
        children: [
            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "subpanel p-3",
                style: {
                    width: "100%"
                },
                children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                    className: "relative",
                    children: [
                        error && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                            className: "p-3 mb-2 text-sm text-red-400 bg-red-900/20 rounded",
                            children: error
                        }),
                        svgContent && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                            className: "p-2 mb-2 text-xs text-gray-300",
                            children: [
                                "Loaded track: ",
                                /*#__PURE__*/ jsx_runtime_.jsx("code", {
                                    children: svgFile
                                })
                            ]
                        }),
                        /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                            className: "relative w-full max-w-[460px] mx-auto aspect-square",
                            children: [
                                svgContent && isReady ? /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                    className: "track-svg absolute inset-0",
                                    dangerouslySetInnerHTML: {
                                        __html: svgContent
                                    }
                                }) : /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                    className: "absolute inset-0 bg-gray-800 flex items-center justify-center text-sm text-gray-400",
                                    children: "Loading track..."
                                }),
                                viewBox && /*#__PURE__*/ jsx_runtime_.jsx("svg", {
                                    viewBox: `${viewBox.minX} ${viewBox.minY} ${viewBox.w} ${viewBox.h}`,
                                    preserveAspectRatio: "xMidYMid meet",
                                    className: "absolute inset-0 w-full h-full pointer-events-none",
                                    children: corners.map((corner)=>/*#__PURE__*/ (0,jsx_runtime_.jsxs)("g", {
                                            children: [
                                                /*#__PURE__*/ jsx_runtime_.jsx("circle", {
                                                    cx: corner.x,
                                                    cy: corner.y,
                                                    r: Math.max(4, Math.min(viewBox.w, viewBox.h) * 0.01),
                                                    fill: corner.type === "slow" ? "#ef4444" : corner.type === "medium" ? "#f59e0b" : "#10b981",
                                                    opacity: 0.85
                                                }),
                                                /*#__PURE__*/ jsx_runtime_.jsx("text", {
                                                    x: corner.x,
                                                    y: corner.y - viewBox.h * 0.02,
                                                    fontSize: Math.max(10, Math.round(viewBox.h * 0.03)),
                                                    fill: "#ffffff",
                                                    textAnchor: "middle",
                                                    children: corner.number
                                                })
                                            ]
                                        }, corner.number))
                                })
                            ]
                        })
                    ]
                })
            }),
            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                className: "mt-2 flex gap-2 justify-center text-sm",
                children: [
                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        className: "flex items-center gap-1",
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                className: "w-3 h-3 rounded-full bg-red-500 opacity-75"
                            }),
                            "Slow"
                        ]
                    }),
                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        className: "flex items-center gap-1",
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                className: "w-3 h-3 rounded-full bg-yellow-500 opacity-75"
                            }),
                            "Medium"
                        ]
                    }),
                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        className: "flex items-center gap-1",
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                className: "w-3 h-3 rounded-full bg-green-500 opacity-75"
                            }),
                            "Fast"
                        ]
                    })
                ]
            })
        ]
    });
}

// EXTERNAL MODULE: ./node_modules/recharts/lib/index.js
var lib = __webpack_require__(83358);
;// CONCATENATED MODULE: ./components/TyreCompoundIcon.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 

const COMPOUND_CONFIG = {
    SOFT: {
        color: "#dc2626",
        letter: "S"
    },
    MEDIUM: {
        color: "#eab308",
        letter: "M"
    },
    HARD: {
        color: "#d1d5db",
        letter: "H"
    },
    INTERMEDIATE: {
        color: "#059669",
        letter: "I"
    },
    WET: {
        color: "#2563eb",
        letter: "W"
    }
};
function TyreCompoundIcon({ compound, size = 18 }) {
    if (!compound) {
        return null;
    }
    const normalized = compound.toUpperCase();
    const config = COMPOUND_CONFIG[normalized] || {
        color: "#6b7280",
        letter: "?"
    };
    return /*#__PURE__*/ jsx_runtime_.jsx("div", {
        className: "inline-flex items-center justify-center rounded-full border-2 border-black font-bold text-black",
        style: {
            width: size,
            height: size,
            backgroundColor: config.color,
            fontSize: size * 0.6,
            lineHeight: 1
        },
        title: compound,
        children: config.letter
    });
}

;// CONCATENATED MODULE: ./components/ChartTooltip.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 


const formatLapTime = (value)=>{
    if (value == null || value === "") return "-";
    if (Array.isArray(value)) {
        return value.map((item)=>formatLapTime(item)).join(", ");
    }
    const numeric = typeof value === "number" ? value : Number(value);
    if (Number.isNaN(numeric)) return "-";
    const minutes = Math.floor(numeric / 60);
    const seconds = numeric % 60;
    const secondsInt = Math.floor(seconds);
    const milliseconds = Math.round((seconds - secondsInt) * 1000);
    if (minutes > 0) {
        // Format: M:SS.mmm
        return `${minutes}:${secondsInt.toString().padStart(2, "0")}.${milliseconds.toString().padStart(3, "0")}`;
    }
    // Format: SS.mmm (no minutes)
    return `${secondsInt}.${milliseconds.toString().padStart(3, "0")}`;
};
function ChartTooltip({ active, payload, label, compoundMap }) {
    if (!active || !payload || !payload.length) {
        return null;
    }
    const lapNumber = typeof label === "number" ? label : Number(label);
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
        className: "panel p-2 min-w-[120px]",
        children: [
            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                className: "mb-1 text-xs font-semibold text-gray-300",
                children: [
                    "Lap ",
                    lapNumber
                ]
            }),
            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "space-y-1",
                children: payload.map((entry, index)=>{
                    const driverCode = String(entry.name || "");
                    const lapTime = entry.value;
                    const compound = compoundMap?.get(lapNumber)?.get(driverCode.toUpperCase()) || null;
                    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        className: "flex items-center gap-2 text-xs",
                        children: [
                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("span", {
                                className: "font-medium",
                                style: {
                                    color: entry.color || "#9aa4b2"
                                },
                                children: [
                                    driverCode,
                                    ":"
                                ]
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                className: "text-gray-200",
                                children: formatLapTime(lapTime)
                            }),
                            compound && /*#__PURE__*/ jsx_runtime_.jsx(TyreCompoundIcon, {
                                compound: compound,
                                size: 16
                            })
                        ]
                    }, index);
                })
            })
        ]
    });
}

;// CONCATENATED MODULE: ./components/F1CarIcon.tsx



/**
 * F1 car icon with team-colored glow
 */ function F1CarIcon({ driverCode, color, size = 16, className = "" }) {
    // Get team color from driver code
    const teamColor = color || driverColorMap[driverCode.toUpperCase()] || "#888888";
    // Create unique filter ID based on driver code AND color to avoid conflicts
    // Use React.useId() to ensure uniqueness per component instance
    const uniqueId = react_default().useId().replace(/:/g, "-");
    const filterId = `car-glow-${driverCode.toUpperCase()}-${teamColor.replace("#", "").replace(/[^a-zA-Z0-9]/g, "")}-${uniqueId}`;
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("g", {
        className: className,
        children: [
            /*#__PURE__*/ jsx_runtime_.jsx("defs", {
                children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)("filter", {
                    id: filterId,
                    x: "-50%",
                    y: "-50%",
                    width: "200%",
                    height: "200%",
                    children: [
                        /*#__PURE__*/ jsx_runtime_.jsx("feGaussianBlur", {
                            in: "SourceAlpha",
                            stdDeviation: "2",
                            result: "blur"
                        }),
                        /*#__PURE__*/ jsx_runtime_.jsx("feFlood", {
                            floodColor: teamColor,
                            result: "glowColor"
                        }),
                        /*#__PURE__*/ jsx_runtime_.jsx("feComposite", {
                            in: "glowColor",
                            in2: "blur",
                            operator: "in",
                            result: "coloredGlow"
                        }),
                        /*#__PURE__*/ (0,jsx_runtime_.jsxs)("feMerge", {
                            children: [
                                /*#__PURE__*/ jsx_runtime_.jsx("feMergeNode", {
                                    in: "coloredGlow"
                                }),
                                /*#__PURE__*/ jsx_runtime_.jsx("feMergeNode", {
                                    in: "SourceGraphic"
                                })
                            ]
                        })
                    ]
                })
            }),
            /*#__PURE__*/ jsx_runtime_.jsx("g", {
                transform: "translate(0, 1.5) rotate(180)",
                opacity: "0.3",
                children: /*#__PURE__*/ jsx_runtime_.jsx("image", {
                    href: "/logos/f1 car.png",
                    x: -size / 2,
                    y: -size / 2,
                    width: size,
                    height: size,
                    preserveAspectRatio: "xMidYMid meet",
                    opacity: "0.5"
                })
            }),
            /*#__PURE__*/ jsx_runtime_.jsx("g", {
                transform: "rotate(180)",
                children: /*#__PURE__*/ jsx_runtime_.jsx("image", {
                    href: "/logos/f1 car.png",
                    x: -size / 2,
                    y: -size / 2,
                    width: size,
                    height: size,
                    preserveAspectRatio: "xMidYMid meet",
                    filter: `url(#${filterId})`
                })
            })
        ]
    });
}

;// CONCATENATED MODULE: ./components/ChartPanel.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 





const FALLBACK_COLORS = [
    "#7cc7ff",
    "#22c55e",
    "#facc15",
    "#f97316",
    "#a855f7",
    "#f87171"
];
const getDriverColor = (code, index)=>{
    const normalized = code.toUpperCase();
    return driverColorMap[normalized] ?? FALLBACK_COLORS[index % FALLBACK_COLORS.length];
};
const buildChartData = (sessionData, driverCodes, showOutliers)=>{
    if (!sessionData || !driverCodes.length) return [];
    const driversSet = new Set(driverCodes.map((code)=>code.toUpperCase()));
    const lapsByNumber = new Map();
    for (const lap of sessionData.laps){
        if (!lap || lap.lapNumber == null) continue;
        if (!showOutliers && lap.isValid === false) continue;
        const normalizedDriver = lap.driver?.toUpperCase();
        if (!driversSet.has(normalizedDriver)) continue;
        const lapNumber = Number(lap.lapNumber);
        if (Number.isNaN(lapNumber)) continue;
        const existing = lapsByNumber.get(lapNumber) ?? {
            lapNumber
        };
        existing[normalizedDriver] = typeof lap.lapTimeSeconds === "number" ? lap.lapTimeSeconds : null;
        lapsByNumber.set(lapNumber, existing);
    }
    return Array.from(lapsByNumber.values()).sort((a, b)=>a.lapNumber - b.lapNumber);
};
const buildQualifyingData = (sessionData, driverCodes, showOutliers)=>{
    if (!sessionData || !driverCodes.length) return [];
    const driversSet = new Set(driverCodes.map((code)=>code.toUpperCase()));
    const attempts = [];
    const attemptCounters = new Map();
    // First pass: collect all valid laps per driver to find best times
    const validLapsByDriver = new Map();
    for (const lap of sessionData.laps){
        if (!lap || lap.lapNumber == null) continue;
        if (lap.isValid !== true) continue;
        const normalizedDriver = lap.driver?.toUpperCase();
        if (!driversSet.has(normalizedDriver)) continue;
        if (typeof lap.lapTimeSeconds !== "number" || Number.isNaN(lap.lapTimeSeconds)) continue;
        if (!validLapsByDriver.has(normalizedDriver)) {
            validLapsByDriver.set(normalizedDriver, []);
        }
        validLapsByDriver.get(normalizedDriver).push(lap.lapTimeSeconds);
    }
    // Calculate best time per driver (for outlier detection)
    const bestTimeByDriver = new Map();
    for (const [driver, times] of validLapsByDriver.entries()){
        bestTimeByDriver.set(driver, Math.min(...times));
    }
    // Sort laps by lap number to maintain attempt order
    const sortedLaps = [
        ...sessionData.laps
    ].sort((a, b)=>{
        const aNum = a.lapNumber ?? 0;
        const bNum = b.lapNumber ?? 0;
        return aNum - bNum;
    });
    for (const lap of sortedLaps){
        if (!lap || lap.lapNumber == null) continue;
        // For qualifying, ONLY count valid laps (officially counted laps)
        // Always exclude out-laps, in-laps, warm-up laps, etc.
        if (lap.isValid !== true) continue;
        const normalizedDriver = lap.driver?.toUpperCase();
        if (!driversSet.has(normalizedDriver)) continue;
        if (typeof lap.lapTimeSeconds !== "number" || Number.isNaN(lap.lapTimeSeconds)) continue;
        // Filter out laps that are significantly slower than the driver's best time
        // This catches slow laps that are marked valid but aren't real qualifying attempts
        const bestTime = bestTimeByDriver.get(normalizedDriver);
        if (bestTime !== undefined) {
            // If a lap is more than 25% slower than the driver's best, exclude it
            // This filters out slow laps that shouldn't be counted as attempts
            const threshold = bestTime * 1.25;
            if (lap.lapTimeSeconds > threshold) {
                continue;
            }
        }
        const currentCount = attemptCounters.get(normalizedDriver) ?? 0;
        attemptCounters.set(normalizedDriver, currentCount + 1);
        attempts.push({
            attemptNumber: currentCount + 1,
            lapTimeSeconds: lap.lapTimeSeconds,
            driver: normalizedDriver,
            isPersonalBest: lap.isPersonalBest ?? false,
            lapNumber: lap.lapNumber,
            compound: lap.compound ?? null,
            sessionTimeSeconds: lap.sessionTimeSeconds ?? null
        });
    }
    return attempts;
};
const buildCompoundMap = (sessionData, driverCodes, showOutliers)=>{
    const compoundMap = new Map();
    if (!sessionData || !driverCodes.length) return compoundMap;
    const driversSet = new Set(driverCodes.map((code)=>code.toUpperCase()));
    for (const lap of sessionData.laps){
        if (!lap || lap.lapNumber == null) continue;
        if (!showOutliers && lap.isValid === false) continue;
        const normalizedDriver = lap.driver?.toUpperCase();
        if (!driversSet.has(normalizedDriver)) continue;
        const lapNumber = Number(lap.lapNumber);
        if (Number.isNaN(lapNumber)) continue;
        let lapMap = compoundMap.get(lapNumber);
        if (!lapMap) {
            lapMap = new Map();
            compoundMap.set(lapNumber, lapMap);
        }
        lapMap.set(normalizedDriver, lap.compound || null);
    }
    return compoundMap;
};
const computeYDomain = (data, drivers)=>{
    let min = Infinity;
    let max = -Infinity;
    data.forEach((entry)=>{
        drivers.forEach((code)=>{
            const value = entry[code.toUpperCase()];
            if (typeof value === "number" && !Number.isNaN(value)) {
                min = Math.min(min, value);
                max = Math.max(max, value);
            }
        });
    });
    if (min === Infinity || max === -Infinity) {
        return undefined;
    }
    const padding = Math.max((max - min) * 0.05, 0.3);
    return [
        Math.max(min - padding, 0),
        max + padding
    ];
};
function ChartPanel({ sessionData, selectedDrivers, loading, showOutliers }) {
    // Toggle for showing all valid times vs only fastest per driver (qualifying only)
    const [showAllValidTimes, setShowAllValidTimes] = react_default().useState(false);
    const normalizedSelectedDrivers = (0,react_.useMemo)(()=>selectedDrivers.map((code)=>code.toUpperCase()), [
        selectedDrivers
    ]);
    const availableDrivers = (0,react_.useMemo)(()=>{
        if (!sessionData) return [];
        const driversFromData = Object.keys(sessionData.drivers ?? {});
        return driversFromData.map((code)=>code.toUpperCase());
    }, [
        sessionData
    ]);
    const driversToDisplay = (0,react_.useMemo)(()=>{
        if (!sessionData) return [];
        const filtered = normalizedSelectedDrivers.filter((code)=>sessionData.drivers?.[code]);
        if (filtered.length > 0) {
            return filtered;
        }
        return availableDrivers.slice(0, 4);
    }, [
        availableDrivers,
        normalizedSelectedDrivers,
        sessionData
    ]);
    // Check if this is a qualifying or sprint qualifying session
    const isQualifyingSession = (0,react_.useMemo)(()=>{
        if (!sessionData) return false;
        const sessionCode = sessionData.meta?.session?.toUpperCase();
        return sessionCode === "Q" || sessionCode === "SQ";
    }, [
        sessionData
    ]);
    const chartData = (0,react_.useMemo)(()=>buildChartData(sessionData, driversToDisplay, showOutliers), [
        sessionData,
        driversToDisplay,
        showOutliers
    ]);
    const qualifyingData = (0,react_.useMemo)(()=>buildQualifyingData(sessionData, driversToDisplay, showOutliers), [
        sessionData,
        driversToDisplay,
        showOutliers
    ]);
    const compoundMap = (0,react_.useMemo)(()=>buildCompoundMap(sessionData, driversToDisplay, showOutliers), [
        sessionData,
        driversToDisplay,
        showOutliers
    ]);
    const yDomain = (0,react_.useMemo)(()=>{
        if (isQualifyingSession && qualifyingData.length > 0) {
            const times = qualifyingData.map((a)=>a.lapTimeSeconds).filter((t)=>typeof t === "number");
            if (times.length === 0) return undefined;
            const min = Math.min(...times);
            const max = Math.max(...times);
            const padding = Math.max((max - min) * 0.05, 0.3);
            return [
                Math.max(min - padding, 0),
                max + padding
            ];
        }
        return computeYDomain(chartData, driversToDisplay);
    }, [
        isQualifyingSession,
        qualifyingData,
        chartData,
        driversToDisplay
    ]);
    // Calculate Q1/Q2/Q3 boundaries using official session_status data from FastF1
    // FastF1 provides session_status with "Started" and "Finished" events for each Q session
    // This is the most reliable way to identify Q boundaries
    const qualifyingBoundaries = (0,react_.useMemo)(()=>{
        if (!isQualifyingSession || qualifyingData.length === 0 || !sessionData) return null;
        // First, try to use official qualifying boundaries from session_status
        if (sessionData.qualifyingBoundaries) {
            const boundaries = sessionData.qualifyingBoundaries;
            // Map session times to attempt numbers
            const attemptByTime = new Map();
            for (const attempt of qualifyingData){
                if (typeof attempt.sessionTimeSeconds === "number" && !Number.isNaN(attempt.sessionTimeSeconds)) {
                    attemptByTime.set(attempt.sessionTimeSeconds, attempt.attemptNumber);
                }
            }
            // Find attempt numbers at boundaries
            let q1End = 0;
            let q2End = 0;
            let q3End = 0;
            // Q1 end
            if (boundaries.q1End !== null) {
                const attemptsInQ1 = Array.from(attemptByTime.entries()).filter(([time])=>time <= boundaries.q1End).map(([, attempt])=>attempt);
                if (attemptsInQ1.length > 0) {
                    q1End = Math.max(...attemptsInQ1) + 0.5;
                }
            }
            // Q2 end
            if (boundaries.q2End !== null && q1End > 0) {
                const attemptsInQ2 = Array.from(attemptByTime.entries()).filter(([time])=>time > boundaries.q1End && time <= boundaries.q2End).map(([, attempt])=>attempt);
                if (attemptsInQ2.length > 0) {
                    q2End = Math.max(...attemptsInQ2) + 0.5;
                }
            }
            // Q3 goes to the end
            if (q2End > 0) {
                const maxAttempt = Math.max(...qualifyingData.map((a)=>a.attemptNumber));
                q3End = maxAttempt + 0.5;
            }
            if (q1End > 0) {
                return {
                    q1End,
                    q2End,
                    q3End
                };
            }
        }
        // Fallback: Use session time gaps if no official boundaries available
        const sessionTimes = qualifyingData.map((a)=>a.sessionTimeSeconds).filter((t)=>typeof t === "number" && !Number.isNaN(t)).sort((a, b)=>a - b);
        if (sessionTimes.length === 0) {
            // Final fallback: attempt-based
            const maxAttempt = Math.max(...qualifyingData.map((a)=>a.attemptNumber));
            if (maxAttempt > 7) {
                return {
                    q1End: 3.5,
                    q2End: 6.5,
                    q3End: Math.min(maxAttempt + 0.5, 9.5)
                };
            } else if (maxAttempt > 4) {
                return {
                    q1End: 3.5,
                    q2End: 6.5,
                    q3End: 0
                };
            } else {
                return {
                    q1End: 3.5,
                    q2End: 0,
                    q3End: 0
                };
            }
        }
        // Fallback logic using gaps (same as before)
        const firstLapTime = sessionTimes[0];
        const gaps = [];
        for(let i = 1; i < sessionTimes.length; i++){
            const gap = sessionTimes[i] - sessionTimes[i - 1];
            if (gap >= 120) {
                gaps.push({
                    beforeTime: sessionTimes[i - 1],
                    afterTime: sessionTimes[i],
                    gap
                });
            }
        }
        gaps.sort((a, b)=>b.gap - a.gap);
        let q1EndTime = null;
        let q2EndTime = null;
        if (gaps.length >= 2) {
            const sortedByTime = [
                ...gaps
            ].sort((a, b)=>a.beforeTime - b.beforeTime);
            q1EndTime = sortedByTime[0].beforeTime;
            q2EndTime = sortedByTime[1].beforeTime;
        } else if (gaps.length === 1) {
            q1EndTime = gaps[0].beforeTime;
            q2EndTime = gaps[0].afterTime + 900;
        } else {
            q1EndTime = firstLapTime + 1080;
            q2EndTime = firstLapTime + 1080 + 150 + 900;
        }
        const attemptByTime = new Map();
        for (const attempt of qualifyingData){
            if (typeof attempt.sessionTimeSeconds === "number" && !Number.isNaN(attempt.sessionTimeSeconds)) {
                attemptByTime.set(attempt.sessionTimeSeconds, attempt.attemptNumber);
            }
        }
        let q1End = 0;
        let q2End = 0;
        let q3End = 0;
        if (q1EndTime !== null) {
            const attemptsInQ1 = Array.from(attemptByTime.entries()).filter(([time])=>time <= q1EndTime).map(([, attempt])=>attempt);
            if (attemptsInQ1.length > 0) {
                q1End = Math.max(...attemptsInQ1) + 0.5;
            } else {
                q1End = 3.5;
            }
        }
        if (q2EndTime !== null && q1End > 0) {
            const attemptsInQ2 = Array.from(attemptByTime.entries()).filter(([time])=>time > q1EndTime && time <= q2EndTime).map(([, attempt])=>attempt);
            if (attemptsInQ2.length > 0) {
                q2End = Math.max(...attemptsInQ2) + 0.5;
            } else {
                q2End = 6.5;
            }
        }
        if (q2End > 0) {
            const maxAttempt = Math.max(...qualifyingData.map((a)=>a.attemptNumber));
            q3End = maxAttempt + 0.5;
        }
        if (q1End === 0) {
            const maxAttempt = Math.max(...qualifyingData.map((a)=>a.attemptNumber));
            if (maxAttempt > 7) {
                return {
                    q1End: 3.5,
                    q2End: 6.5,
                    q3End: Math.min(maxAttempt + 0.5, 9.5)
                };
            } else if (maxAttempt > 4) {
                return {
                    q1End: 3.5,
                    q2End: 6.5,
                    q3End: 0
                };
            } else {
                return {
                    q1End: 3.5,
                    q2End: 0,
                    q3End: 0
                };
            }
        }
        return {
            q1End,
            q2End,
            q3End
        };
    }, [
        isQualifyingSession,
        qualifyingData,
        sessionData
    ]);
    // Find fastest time in each Q segment (across ALL drivers, not just selected)
    const fastestTimesBySegment = (0,react_.useMemo)(()=>{
        if (!isQualifyingSession || !sessionData) return {
            Q1: null,
            Q2: null,
            Q3: null
        };
        // Get all qualifying attempts from all drivers
        const allDrivers = Object.keys(sessionData.drivers || {});
        const allQualifyingData = buildQualifyingData(sessionData, allDrivers, showOutliers);
        // Group by Q segment and find fastest in each
        const fastest = {
            Q1: null,
            Q2: null,
            Q3: null
        };
        for (const attempt of allQualifyingData){
            if (!attempt.qSegment) continue;
            const current = fastest[attempt.qSegment];
            if (!current || attempt.lapTimeSeconds < current.lapTimeSeconds) {
                fastest[attempt.qSegment] = attempt;
            }
        }
        return fastest;
    }, [
        isQualifyingSession,
        sessionData,
        showOutliers
    ]);
    // Assign Q segments to attempts based on session time and group by driver
    // Also add collision detection offsets and fastest time markers
    const qualifyingByDriver = (0,react_.useMemo)(()=>{
        if (!isQualifyingSession) return {};
        // First, assign Q segments and X positions to each attempt based on session time
        const attemptsWithSegments = qualifyingData.map((attempt)=>{
            let qSegment = "Q1";
            let xPosition = 1 // Default to Q1
            ;
            if (qualifyingBoundaries && typeof attempt.sessionTimeSeconds === "number" && sessionData?.qualifyingBoundaries) {
                // Use official qualifying boundaries from session_status
                const boundaries = sessionData.qualifyingBoundaries;
                const sessionTime = attempt.sessionTimeSeconds;
                // Assign segment based on official boundaries
                if (boundaries.q1End !== null && sessionTime <= boundaries.q1End) {
                    qSegment = "Q1";
                    xPosition = 1;
                } else if (boundaries.q2End !== null && sessionTime <= boundaries.q2End) {
                    qSegment = "Q2";
                    xPosition = 2;
                } else {
                    qSegment = "Q3";
                    xPosition = 3;
                }
            } else if (qualifyingBoundaries && typeof attempt.sessionTimeSeconds === "number") {
                // Fallback: Use session time gaps if no official boundaries
                const sessionTime = attempt.sessionTimeSeconds;
                const sessionTimes = qualifyingData.map((a)=>a.sessionTimeSeconds).filter((t)=>typeof t === "number" && !Number.isNaN(t)).sort((a, b)=>a - b);
                if (sessionTimes.length > 0) {
                    const firstLapTime = sessionTimes[0];
                    const gaps = [];
                    for(let i = 1; i < sessionTimes.length; i++){
                        const gap = sessionTimes[i] - sessionTimes[i - 1];
                        if (gap >= 120) {
                            gaps.push({
                                beforeTime: sessionTimes[i - 1],
                                afterTime: sessionTimes[i],
                                gap
                            });
                        }
                    }
                    gaps.sort((a, b)=>b.gap - a.gap);
                    let q1EndTime = null;
                    let q2EndTime = null;
                    if (gaps.length >= 2) {
                        const sortedByTime = [
                            ...gaps
                        ].sort((a, b)=>a.beforeTime - b.beforeTime);
                        q1EndTime = sortedByTime[0].beforeTime;
                        q2EndTime = sortedByTime[1].beforeTime;
                    } else if (gaps.length === 1) {
                        q1EndTime = gaps[0].beforeTime;
                        q2EndTime = gaps[0].afterTime + 900;
                    } else {
                        q1EndTime = firstLapTime + 1080;
                        q2EndTime = firstLapTime + 1080 + 150 + 900;
                    }
                    if (q1EndTime !== null && sessionTime <= q1EndTime) {
                        qSegment = "Q1";
                        xPosition = 1;
                    } else if (q2EndTime !== null && sessionTime <= q2EndTime) {
                        qSegment = "Q2";
                        xPosition = 2;
                    } else {
                        qSegment = "Q3";
                        xPosition = 3;
                    }
                } else {
                    // Fallback to attempt number
                    if (qualifyingBoundaries.q1End > 0 && attempt.attemptNumber <= qualifyingBoundaries.q1End) {
                        qSegment = "Q1";
                        xPosition = 1;
                    } else if (qualifyingBoundaries.q2End > 0 && attempt.attemptNumber <= qualifyingBoundaries.q2End) {
                        qSegment = "Q2";
                        xPosition = 2;
                    } else {
                        qSegment = "Q3";
                        xPosition = 3;
                    }
                }
            } else if (qualifyingBoundaries) {
                // Fallback to attempt number if no session time
                if (qualifyingBoundaries.q1End > 0 && attempt.attemptNumber <= qualifyingBoundaries.q1End) {
                    qSegment = "Q1";
                    xPosition = 1;
                } else if (qualifyingBoundaries.q2End > 0 && attempt.attemptNumber <= qualifyingBoundaries.q2End) {
                    qSegment = "Q2";
                    xPosition = 2;
                } else {
                    qSegment = "Q3";
                    xPosition = 3;
                }
            }
            // Check if this is the fastest time in its segment
            const isFastest = fastestTimesBySegment[qSegment]?.driver === attempt.driver && fastestTimesBySegment[qSegment]?.lapTimeSeconds === attempt.lapTimeSeconds;
            return {
                ...attempt,
                qSegment,
                xPosition,
                offsetX: 0,
                offsetY: 0,
                isFastestInSegment: isFastest
            };
        });
        // Collision detection: prevent overlapping when times are close
        // Group attempts by Q segment and sort by lap time
        const attemptsBySegment = new Map();
        for (const attempt of attemptsWithSegments){
            if (!attempt.qSegment) continue;
            if (!attemptsBySegment.has(attempt.qSegment)) {
                attemptsBySegment.set(attempt.qSegment, []);
            }
            attemptsBySegment.get(attempt.qSegment).push(attempt);
        }
        // Process each segment separately
        const COLLISION_THRESHOLD = 0.15 // seconds - if times are within 0.15s, offset them
        ;
        const OFFSET_STEP = 0.12 // X offset step for collision avoidance
        ;
        for (const [segment, attempts] of attemptsBySegment.entries()){
            // Sort by lap time
            attempts.sort((a, b)=>a.lapTimeSeconds - b.lapTimeSeconds);
            // Check for collisions and apply offsets
            for(let i = 0; i < attempts.length; i++){
                const current = attempts[i];
                let offsetX = 0;
                let offsetY = 0;
                let collisionCount = 0;
                // Check collisions with all other attempts in the same segment
                for(let j = 0; j < attempts.length; j++){
                    if (i === j) continue;
                    const other = attempts[j];
                    const timeDiff = Math.abs(current.lapTimeSeconds - other.lapTimeSeconds);
                    if (timeDiff < COLLISION_THRESHOLD) {
                        collisionCount++;
                        // Determine offset direction based on which car is faster
                        const isFaster = current.lapTimeSeconds < other.lapTimeSeconds;
                        const direction = isFaster ? -1 : 1;
                        // Apply offset - alternate left/right for multiple collisions
                        offsetX += direction * OFFSET_STEP * (collisionCount % 2 === 0 ? 1 : -1);
                        // Small vertical offset for visual separation
                        offsetY += (collisionCount % 2 === 0 ? 1 : -1) * 0.015;
                    }
                }
                // Limit offsets to prevent cars from going too far
                offsetX = Math.max(-0.3, Math.min(0.3, offsetX));
                offsetY = Math.max(-0.05, Math.min(0.05, offsetY));
                current.offsetX = offsetX;
                current.offsetY = offsetY;
            }
        }
        // Group by driver
        const grouped = {};
        for (const attempt of attemptsWithSegments){
            if (!grouped[attempt.driver]) {
                grouped[attempt.driver] = [];
            }
            grouped[attempt.driver].push(attempt);
        }
        return grouped;
    }, [
        isQualifyingSession,
        qualifyingData,
        qualifyingBoundaries,
        fastestTimesBySegment
    ]);
    const showNoSelectionMessage = !normalizedSelectedDrivers.length || normalizedSelectedDrivers.length > 0 && !driversToDisplay.some((code)=>normalizedSelectedDrivers.includes(code));
    // Filter qualifying data based on toggle
    const filteredQualifyingByDriver = (0,react_.useMemo)(()=>{
        if (!isQualifyingSession || showAllValidTimes) {
            return qualifyingByDriver;
        }
        // When toggle is off, only show fastest time per driver per Q segment
        const filtered = {};
        for (const [driver, attempts] of Object.entries(qualifyingByDriver)){
            // Group by Q segment and find fastest in each
            const bySegment = {
                Q1: [],
                Q2: [],
                Q3: []
            };
            for (const attempt of attempts){
                if (attempt.qSegment) {
                    bySegment[attempt.qSegment].push(attempt);
                }
            }
            // Get fastest from each segment
            const fastest = [];
            for (const segment of [
                "Q1",
                "Q2",
                "Q3"
            ]){
                if (bySegment[segment].length > 0) {
                    const fastestInSegment = bySegment[segment].reduce((best, current)=>current.lapTimeSeconds < best.lapTimeSeconds ? current : best);
                    fastest.push(fastestInSegment);
                }
            }
            filtered[driver] = fastest;
        }
        return filtered;
    }, [
        qualifyingByDriver,
        isQualifyingSession,
        showAllValidTimes
    ]);
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
        className: "mt-6 panel p-4",
        children: [
            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                className: "mb-4 flex items-center justify-between gap-3",
                children: [
                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx("h2", {
                                className: "text-lg font-semibold text-gray-100",
                                children: "Lap Time Comparison"
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx("p", {
                                className: "text-xs text-gray-500",
                                children: isQualifyingSession ? "Qualifying attempts shown as scatter plot. Best laps highlighted in yellow. Attempts numbered sequentially per driver." : "Lap times pulled from FastF1 telemetry. Values plotted in seconds."
                            })
                        ]
                    }),
                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        className: "flex items-center gap-4",
                        children: [
                            isQualifyingSession && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("label", {
                                className: "flex items-center gap-2 text-xs text-gray-400 cursor-pointer",
                                children: [
                                    /*#__PURE__*/ jsx_runtime_.jsx("input", {
                                        type: "checkbox",
                                        checked: showAllValidTimes,
                                        onChange: (e)=>setShowAllValidTimes(e.target.checked),
                                        className: "h-4 w-4 rounded border-gray-600 bg-gray-800 text-accent focus:ring-accent focus:ring-offset-gray-900"
                                    }),
                                    /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                        children: "Show all valid times during session"
                                    })
                                ]
                            }),
                            sessionData?.meta?.event?.name && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                className: "text-right text-xs text-gray-400",
                                children: [
                                    /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                        children: sessionData.meta.event.name
                                    }),
                                    sessionData.meta.event.country && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                        children: sessionData.meta.event.country
                                    })
                                ]
                            })
                        ]
                    })
                ]
            }),
            loading && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "flex h-64 items-center justify-center text-sm text-gray-400",
                children: "Loading session telemetry…"
            }),
            !loading && !sessionData && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "flex h-64 items-center justify-center text-sm text-gray-400",
                children: "Select a track and session to load telemetry."
            }),
            !loading && sessionData && showNoSelectionMessage && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                className: "flex h-64 flex-col items-center justify-center gap-2 text-center text-sm text-gray-400",
                children: [
                    /*#__PURE__*/ jsx_runtime_.jsx("span", {
                        children: "Telemetry for the selected drivers is not available in the current dataset. Choose another driver or regenerate data."
                    }),
                    availableDrivers.length > 0 && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("span", {
                        className: "text-xs text-gray-500",
                        children: [
                            "Available drivers: ",
                            availableDrivers.join(", ")
                        ]
                    })
                ]
            }),
            !loading && sessionData && !chartData.length && !qualifyingData.length && !showNoSelectionMessage && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "flex h-64 items-center justify-center text-sm text-gray-400",
                children: "No lap times to display for the current selection."
            }),
            !loading && sessionData && isQualifyingSession && qualifyingData.length > 0 && !showNoSelectionMessage && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "h-[360px]",
                children: /*#__PURE__*/ jsx_runtime_.jsx(lib/* ResponsiveContainer */.h2, {
                    width: "100%",
                    height: "100%",
                    children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)(lib/* ScatterChart */.GN, {
                        margin: {
                            top: 12,
                            right: 12,
                            left: 6,
                            bottom: 6
                        },
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* CartesianGrid */.q3, {
                                strokeDasharray: "3 6",
                                className: "chart-grid"
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* XAxis */.Kc, {
                                type: "number",
                                dataKey: "xPosition",
                                name: "Qualifying Segment",
                                stroke: "#9aa4b2",
                                domain: [
                                    0.5,
                                    3.5
                                ],
                                ticks: [
                                    1,
                                    2,
                                    3
                                ],
                                tickFormatter: (value)=>{
                                    if (value === 1) return "Q1";
                                    if (value === 2) return "Q2";
                                    if (value === 3) return "Q3";
                                    return "";
                                }
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* YAxis */.B2, {
                                type: "number",
                                dataKey: "lapTimeSeconds",
                                name: "Lap Time",
                                stroke: "#9aa4b2",
                                domain: yDomain,
                                width: 60,
                                tickFormatter: (value)=>value.toFixed(1),
                                label: {
                                    value: "Lap Time (s)",
                                    angle: -90,
                                    position: "insideLeft"
                                }
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* ReferenceArea */.zu, {
                                x1: 0.5,
                                x2: 1.5,
                                fill: "#ef4444",
                                fillOpacity: 0.1,
                                stroke: "#ef4444",
                                strokeWidth: 2,
                                strokeOpacity: 0.6
                            }),
                            qualifyingBoundaries && qualifyingBoundaries.q2End > 0 && /*#__PURE__*/ jsx_runtime_.jsx(lib/* ReferenceArea */.zu, {
                                x1: 1.5,
                                x2: 2.5,
                                fill: "#3b82f6",
                                fillOpacity: 0.1,
                                stroke: "#3b82f6",
                                strokeWidth: 2,
                                strokeOpacity: 0.6
                            }),
                            qualifyingBoundaries && qualifyingBoundaries.q3End > 0 && /*#__PURE__*/ jsx_runtime_.jsx(lib/* ReferenceArea */.zu, {
                                x1: 2.5,
                                x2: 3.5,
                                fill: "#22c55e",
                                fillOpacity: 0.1,
                                stroke: "#22c55e",
                                strokeWidth: 2,
                                strokeOpacity: 0.6
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* Tooltip */.u, {
                                cursor: {
                                    strokeDasharray: "3 3"
                                },
                                content: ({ active, payload })=>{
                                    if (!active || !payload || !payload[0]) return null;
                                    const data = payload[0].payload;
                                    const formatTime = (seconds)=>{
                                        const mins = Math.floor(seconds / 60);
                                        const secs = (seconds % 60).toFixed(3);
                                        return mins > 0 ? `${mins}:${secs.padStart(6, "0")}` : `${secs}s`;
                                    };
                                    // Get original lap time (before offset)
                                    const originalTime = data.lapTimeSeconds - (data.offsetY || 0);
                                    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                        className: "panel p-2 min-w-[140px] animate-in fade-in-0 zoom-in-95 duration-200",
                                        children: [
                                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                className: "text-xs font-semibold text-gray-300 mb-1",
                                                children: [
                                                    data.driver,
                                                    " - ",
                                                    data.qSegment || "Q1"
                                                ]
                                            }),
                                            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                                className: "text-xs text-gray-200 font-mono",
                                                children: formatTime(originalTime)
                                            }),
                                            data.isFastestInSegment && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                className: "text-xs text-yellow-400 font-medium mt-1 flex items-center gap-1",
                                                children: [
                                                    "\uD83C\uDFC1 Fastest in ",
                                                    data.qSegment
                                                ]
                                            }),
                                            data.isPersonalBest && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                                className: "text-xs text-accent font-medium mt-1",
                                                children: "⭐ Personal Best"
                                            }),
                                            data.compound && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                className: "text-xs text-gray-400 mt-1",
                                                children: [
                                                    "Compound: ",
                                                    data.compound
                                                ]
                                            })
                                        ]
                                    });
                                }
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* Legend */.De, {}),
                            fastestTimesBySegment.Q1 && (()=>{
                                const fastest = fastestTimesBySegment.Q1;
                                // Find the attempt in the displayed data to get its offset
                                const displayedAttempt = filteredQualifyingByDriver[fastest.driver]?.find((a)=>a.lapTimeSeconds === fastest.lapTimeSeconds && a.qSegment === "Q1");
                                const offsetX = displayedAttempt?.offsetX || 0;
                                const offsetY = displayedAttempt?.offsetY || 0;
                                return /*#__PURE__*/ jsx_runtime_.jsx(lib/* ReferenceLine */.db, {
                                    x: 1 + offsetX,
                                    y: fastest.lapTimeSeconds + offsetY,
                                    stroke: "none",
                                    label: {
                                        value: "\uD83C\uDFC1",
                                        position: "top",
                                        offset: 12,
                                        style: {
                                            fontSize: "18px",
                                            fontWeight: "bold"
                                        }
                                    }
                                });
                            })(),
                            fastestTimesBySegment.Q2 && (()=>{
                                const fastest = fastestTimesBySegment.Q2;
                                const displayedAttempt = filteredQualifyingByDriver[fastest.driver]?.find((a)=>a.lapTimeSeconds === fastest.lapTimeSeconds && a.qSegment === "Q2");
                                const offsetX = displayedAttempt?.offsetX || 0;
                                const offsetY = displayedAttempt?.offsetY || 0;
                                return /*#__PURE__*/ jsx_runtime_.jsx(lib/* ReferenceLine */.db, {
                                    x: 2 + offsetX,
                                    y: fastest.lapTimeSeconds + offsetY,
                                    stroke: "none",
                                    label: {
                                        value: "\uD83C\uDFC1",
                                        position: "top",
                                        offset: 12,
                                        style: {
                                            fontSize: "18px",
                                            fontWeight: "bold"
                                        }
                                    }
                                });
                            })(),
                            fastestTimesBySegment.Q3 && (()=>{
                                const fastest = fastestTimesBySegment.Q3;
                                const displayedAttempt = filteredQualifyingByDriver[fastest.driver]?.find((a)=>a.lapTimeSeconds === fastest.lapTimeSeconds && a.qSegment === "Q3");
                                const offsetX = displayedAttempt?.offsetX || 0;
                                const offsetY = displayedAttempt?.offsetY || 0;
                                return /*#__PURE__*/ jsx_runtime_.jsx(lib/* ReferenceLine */.db, {
                                    x: 3 + offsetX,
                                    y: fastest.lapTimeSeconds + offsetY,
                                    stroke: "none",
                                    label: {
                                        value: "\uD83C\uDFC1",
                                        position: "top",
                                        offset: 12,
                                        style: {
                                            fontSize: "18px",
                                            fontWeight: "bold"
                                        }
                                    }
                                });
                            })(),
                            driversToDisplay.map((code, index)=>{
                                const driverAttempts = filteredQualifyingByDriver[code] || [];
                                const driverColor = getDriverColor(code, index);
                                return /*#__PURE__*/ jsx_runtime_.jsx(lib/* Scatter */.bp, {
                                    name: code,
                                    data: driverAttempts.map((attempt)=>({
                                            ...attempt,
                                            xPosition: (attempt.xPosition || 1) + (attempt.offsetX || 0),
                                            lapTimeSeconds: attempt.lapTimeSeconds + (attempt.offsetY || 0)
                                        })),
                                    fill: driverColor,
                                    shape: (props)=>{
                                        const { cx, cy, payload } = props;
                                        const attempt = payload;
                                        const carSize = attempt.isPersonalBest ? 28 : 26;
                                        return /*#__PURE__*/ jsx_runtime_.jsx("g", {
                                            transform: `translate(${cx},${cy})`,
                                            style: {
                                                cursor: "pointer",
                                                pointerEvents: "all"
                                            },
                                            children: /*#__PURE__*/ jsx_runtime_.jsx(F1CarIcon, {
                                                driverCode: code,
                                                color: attempt.isPersonalBest ? "#facc15" : driverColor,
                                                size: carSize,
                                                className: "drop-shadow-sm"
                                            })
                                        });
                                    },
                                    isAnimationActive: false
                                }, code);
                            })
                        ]
                    })
                })
            }),
            !loading && sessionData && !isQualifyingSession && chartData.length > 0 && !showNoSelectionMessage && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "h-[360px]",
                children: /*#__PURE__*/ jsx_runtime_.jsx(lib/* ResponsiveContainer */.h2, {
                    width: "100%",
                    height: "100%",
                    children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)(lib/* LineChart */.wW, {
                        data: chartData,
                        margin: {
                            top: 12,
                            right: 12,
                            left: 6,
                            bottom: 6
                        },
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* CartesianGrid */.q3, {
                                strokeDasharray: "3 6",
                                className: "chart-grid"
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* XAxis */.Kc, {
                                dataKey: "lapNumber",
                                stroke: "#9aa4b2",
                                label: {
                                    value: "Lap",
                                    position: "insideBottomRight",
                                    offset: -4
                                }
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* YAxis */.B2, {
                                stroke: "#9aa4b2",
                                domain: yDomain,
                                width: 60,
                                tickFormatter: (value)=>value.toFixed(1)
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* Tooltip */.u, {
                                content: /*#__PURE__*/ jsx_runtime_.jsx(ChartTooltip, {
                                    compoundMap: compoundMap
                                })
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* Legend */.De, {}),
                            driversToDisplay.map((code, index)=>/*#__PURE__*/ jsx_runtime_.jsx(lib/* Line */.x1, {
                                    name: code,
                                    type: "monotone",
                                    dataKey: code,
                                    stroke: getDriverColor(code, index),
                                    strokeWidth: 2,
                                    dot: false,
                                    isAnimationActive: false,
                                    connectNulls: true
                                }, code))
                        ]
                    })
                })
            })
        ]
    });
}

;// CONCATENATED MODULE: ./components/CornerTable.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 

const typeColors = {
    slow: "#ef4444",
    medium: "#eab308",
    fast: "#22c55e"
};
function CornerTable({ corners, cornerInfo, selectedDrivers }) {
    const hasCornerData = Object.values(corners ?? {}).some((driverCorners)=>Array.isArray(driverCorners) && driverCorners.length > 0);
    // Aggregate corner data by corner number
    const aggregatedCorners = (0,react_.useMemo)(()=>{
        const aggregated = new Map();
        // Initialize with track corner info
        cornerInfo.forEach((corner)=>{
            aggregated.set(corner.number, {
                cornerNumber: corner.number,
                cornerType: corner.type,
                driverData: {}
            });
        });
        // Aggregate data from each driver
        selectedDrivers.forEach((driverCode)=>{
            const driverCorners = corners[driverCode] || [];
            // Group by corner number
            const byCorner = new Map();
            driverCorners.forEach((corner)=>{
                const num = corner.cornerNumber;
                if (!byCorner.has(num)) {
                    byCorner.set(num, []);
                }
                byCorner.get(num).push(corner);
            });
            // Calculate aggregates for each corner
            byCorner.forEach((cornerLaps, cornerNum)=>{
                if (!aggregated.has(cornerNum)) {
                    // Corner not in track definition - use detected type or default
                    const detectedType = cornerLaps[0]?.cornerType || "medium";
                    aggregated.set(cornerNum, {
                        cornerNumber: cornerNum,
                        cornerType: detectedType === "unknown" ? "medium" : detectedType,
                        driverData: {}
                    });
                }
                const corner = aggregated.get(cornerNum);
                const validTimes = cornerLaps.map((c)=>c.cornerTime).filter((t)=>t !== null && !isNaN(t));
                const avgEntrySpeed = cornerLaps.reduce((sum, c)=>sum + c.entrySpeed, 0) / cornerLaps.length;
                const avgApexSpeed = cornerLaps.reduce((sum, c)=>sum + c.apexSpeed, 0) / cornerLaps.length;
                const avgExitSpeed = cornerLaps.reduce((sum, c)=>sum + c.exitSpeed, 0) / cornerLaps.length;
                const avgCornerTime = validTimes.length > 0 ? validTimes.reduce((sum, t)=>sum + t, 0) / validTimes.length : null;
                const bestTimeIndex = validTimes.length > 0 ? validTimes.indexOf(Math.min(...validTimes)) : -1;
                corner.driverData[driverCode] = {
                    avgEntrySpeed: Math.round(avgEntrySpeed * 10) / 10,
                    avgApexSpeed: Math.round(avgApexSpeed * 10) / 10,
                    avgExitSpeed: Math.round(avgExitSpeed * 10) / 10,
                    avgCornerTime: avgCornerTime ? Math.round(avgCornerTime * 1000) / 1000 : null,
                    bestCornerTime: bestTimeIndex >= 0 ? Math.round(validTimes[bestTimeIndex] * 1000) / 1000 : null,
                    bestLapNumber: bestTimeIndex >= 0 ? cornerLaps[bestTimeIndex].lapNumber : null,
                    lapCount: cornerLaps.length
                };
            });
        });
        return Array.from(aggregated.values()).sort((a, b)=>a.cornerNumber - b.cornerNumber);
    }, [
        corners,
        cornerInfo,
        selectedDrivers
    ]);
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
        className: "mt-6 panel p-4",
        children: [
            /*#__PURE__*/ jsx_runtime_.jsx("h2", {
                className: "text-lg font-semibold text-gray-100",
                children: "Corner Analysis"
            }),
            /*#__PURE__*/ jsx_runtime_.jsx("p", {
                className: "text-xs text-gray-500",
                children: "Corner-level telemetry showing average speeds and corner times. Best times are highlighted in yellow."
            }),
            !hasCornerData && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "mt-4 rounded border border-dashed border-gray-600/60 bg-gray-900/40 p-4 text-sm text-gray-400",
                children: "Corner telemetry is not yet generated for this session. Generate the dataset with corner detection enabled."
            }),
            hasCornerData && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "mt-4 overflow-x-auto",
                children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)("table", {
                    className: "min-w-full table-auto text-left text-sm",
                    children: [
                        /*#__PURE__*/ jsx_runtime_.jsx("thead", {
                            children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)("tr", {
                                className: "text-gray-400",
                                children: [
                                    /*#__PURE__*/ jsx_runtime_.jsx("th", {
                                        className: "pb-2",
                                        children: "Corner"
                                    }),
                                    /*#__PURE__*/ jsx_runtime_.jsx("th", {
                                        className: "pb-2",
                                        children: "Type"
                                    }),
                                    selectedDrivers.map((code)=>/*#__PURE__*/ jsx_runtime_.jsx("th", {
                                            className: "pb-2 text-center",
                                            children: code
                                        }, code))
                                ]
                            })
                        }),
                        /*#__PURE__*/ jsx_runtime_.jsx("tbody", {
                            children: aggregatedCorners.map((corner)=>/*#__PURE__*/ (0,jsx_runtime_.jsxs)("tr", {
                                    className: "border-t border-[var(--border-clr)]",
                                    children: [
                                        /*#__PURE__*/ jsx_runtime_.jsx("td", {
                                            className: "py-2 font-medium text-gray-200",
                                            children: corner.cornerNumber
                                        }),
                                        /*#__PURE__*/ (0,jsx_runtime_.jsxs)("td", {
                                            className: "py-2",
                                            children: [
                                                /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                    className: "mr-2 inline-block h-2 w-2 rounded-full",
                                                    style: {
                                                        backgroundColor: typeColors[corner.cornerType]
                                                    }
                                                }),
                                                /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                    className: "uppercase tracking-wide text-xs text-gray-400",
                                                    children: corner.cornerType
                                                })
                                            ]
                                        }),
                                        selectedDrivers.map((code)=>{
                                            const data = corner.driverData[code];
                                            if (!data || data.lapCount === 0) {
                                                return /*#__PURE__*/ jsx_runtime_.jsx("td", {
                                                    className: "py-2 text-gray-500 text-xs text-center",
                                                    children: "No data"
                                                }, code);
                                            }
                                            return /*#__PURE__*/ jsx_runtime_.jsx("td", {
                                                className: "py-2 text-gray-300",
                                                children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                    className: "text-xs space-y-1",
                                                    children: [
                                                        data.avgCornerTime !== null && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                            className: "font-mono text-center",
                                                            children: [
                                                                /*#__PURE__*/ (0,jsx_runtime_.jsxs)("span", {
                                                                    className: data.bestCornerTime !== null && data.bestCornerTime < data.avgCornerTime ? "text-yellow-400" : "",
                                                                    children: [
                                                                        data.avgCornerTime.toFixed(3),
                                                                        "s"
                                                                    ]
                                                                }),
                                                                data.bestCornerTime !== null && data.bestCornerTime < data.avgCornerTime && /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                                    className: "ml-1 text-yellow-400 text-[10px]",
                                                                    title: `Best: ${data.bestCornerTime.toFixed(3)}s (Lap ${data.bestLapNumber})`,
                                                                    children: "⭐"
                                                                })
                                                            ]
                                                        }),
                                                        /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                            className: "text-[10px] text-gray-500 text-center space-y-0.5",
                                                            children: [
                                                                /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                                    children: [
                                                                        "Entry: ",
                                                                        data.avgEntrySpeed.toFixed(0),
                                                                        " km/h"
                                                                    ]
                                                                }),
                                                                /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                                    children: [
                                                                        "Apex: ",
                                                                        data.avgApexSpeed.toFixed(0),
                                                                        " km/h"
                                                                    ]
                                                                }),
                                                                /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                                    children: [
                                                                        "Exit: ",
                                                                        data.avgExitSpeed.toFixed(0),
                                                                        " km/h"
                                                                    ]
                                                                }),
                                                                /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                                    className: "text-[9px] text-gray-600 mt-1",
                                                                    children: [
                                                                        data.lapCount,
                                                                        " lap",
                                                                        data.lapCount !== 1 ? "s" : ""
                                                                    ]
                                                                })
                                                            ]
                                                        })
                                                    ]
                                                })
                                            }, code);
                                        })
                                    ]
                                }, corner.cornerNumber))
                        })
                    ]
                })
            })
        ]
    });
}

;// CONCATENATED MODULE: ./components/CornerDeltaChart.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 


function CornerDeltaChart({ corners, cornerInfo, selectedDrivers }) {
    const deltaData = (0,react_.useMemo)(()=>{
        if (selectedDrivers.length < 2) return [];
        const driver1 = selectedDrivers[0];
        const driver2 = selectedDrivers[1];
        const driver1Corners = corners[driver1] || [];
        const driver2Corners = corners[driver2] || [];
        // Group corners by corner number
        const driver1ByCorner = new Map();
        const driver2ByCorner = new Map();
        driver1Corners.forEach((corner)=>{
            const num = corner.cornerNumber;
            if (!driver1ByCorner.has(num)) {
                driver1ByCorner.set(num, []);
            }
            driver1ByCorner.get(num).push(corner);
        });
        driver2Corners.forEach((corner)=>{
            const num = corner.cornerNumber;
            if (!driver2ByCorner.has(num)) {
                driver2ByCorner.set(num, []);
            }
            driver2ByCorner.get(num).push(corner);
        });
        // Calculate average corner times and deltas
        const deltas = [];
        const allCornerNumbers = new Set([
            ...driver1ByCorner.keys(),
            ...driver2ByCorner.keys()
        ]);
        allCornerNumbers.forEach((cornerNum)=>{
            const driver1Laps = driver1ByCorner.get(cornerNum) || [];
            const driver2Laps = driver2ByCorner.get(cornerNum) || [];
            // Calculate average corner times
            const driver1Times = driver1Laps.map((c)=>c.cornerTime).filter((t)=>t !== null && !isNaN(t));
            const driver2Times = driver2Laps.map((c)=>c.cornerTime).filter((t)=>t !== null && !isNaN(t));
            if (driver1Times.length === 0 || driver2Times.length === 0) {
                return;
            }
            const driver1Avg = driver1Times.reduce((sum, t)=>sum + t, 0) / driver1Times.length;
            const driver2Avg = driver2Times.reduce((sum, t)=>sum + t, 0) / driver2Times.length;
            // Delta: positive means driver1 is slower
            const delta = driver1Avg - driver2Avg;
            // Get corner type from cornerInfo or use 'medium' as default
            const cornerInfoItem = cornerInfo.find((c)=>c.number === cornerNum);
            const cornerType = cornerInfoItem?.type || "medium";
            deltas.push({
                cornerNumber: cornerNum,
                cornerType,
                delta: Math.round(delta * 1000) / 1000,
                driver1Time: Math.round(driver1Avg * 1000) / 1000,
                driver2Time: Math.round(driver2Avg * 1000) / 1000
            });
        });
        return deltas.sort((a, b)=>a.cornerNumber - b.cornerNumber);
    }, [
        corners,
        cornerInfo,
        selectedDrivers
    ]);
    if (selectedDrivers.length < 2) {
        return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
            className: "mt-6 panel p-4",
            children: [
                /*#__PURE__*/ jsx_runtime_.jsx("h2", {
                    className: "text-lg font-semibold text-gray-100",
                    children: "Corner Delta Comparison"
                }),
                /*#__PURE__*/ jsx_runtime_.jsx("p", {
                    className: "text-xs text-gray-500 mt-2",
                    children: "Select at least two drivers to compare corner performance."
                })
            ]
        });
    }
    if (deltaData.length === 0) {
        return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
            className: "mt-6 panel p-4",
            children: [
                /*#__PURE__*/ jsx_runtime_.jsx("h2", {
                    className: "text-lg font-semibold text-gray-100",
                    children: "Corner Delta Comparison"
                }),
                /*#__PURE__*/ jsx_runtime_.jsx("p", {
                    className: "text-xs text-gray-500 mt-2",
                    children: "No corner data available for comparison."
                })
            ]
        });
    }
    const driver1 = selectedDrivers[0];
    const driver2 = selectedDrivers[1];
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
        className: "mt-6 panel p-4",
        children: [
            /*#__PURE__*/ jsx_runtime_.jsx("h2", {
                className: "text-lg font-semibold text-gray-100",
                children: "Corner Delta Comparison"
            }),
            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("p", {
                className: "text-xs text-gray-500",
                children: [
                    "Time difference per corner: ",
                    driver1,
                    " vs ",
                    driver2,
                    ". Positive values mean ",
                    driver1,
                    " is slower."
                ]
            }),
            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                className: "mt-4 h-64",
                children: /*#__PURE__*/ jsx_runtime_.jsx(lib/* ResponsiveContainer */.h2, {
                    width: "100%",
                    height: "100%",
                    children: /*#__PURE__*/ (0,jsx_runtime_.jsxs)(lib/* BarChart */.vz, {
                        data: deltaData,
                        margin: {
                            top: 12,
                            right: 12,
                            left: 6,
                            bottom: 6
                        },
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* CartesianGrid */.q3, {
                                strokeDasharray: "3 6",
                                className: "chart-grid"
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* XAxis */.Kc, {
                                dataKey: "cornerNumber",
                                stroke: "#9aa4b2",
                                label: {
                                    value: "Corner",
                                    position: "insideBottomRight",
                                    offset: -4
                                }
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* YAxis */.B2, {
                                stroke: "#9aa4b2",
                                label: {
                                    value: "Delta (s)",
                                    angle: -90,
                                    position: "insideLeft"
                                },
                                tickFormatter: (value)=>value.toFixed(3)
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* Tooltip */.u, {
                                content: ({ active, payload })=>{
                                    if (!active || !payload || !payload[0]) return null;
                                    const data = payload[0].payload;
                                    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                        className: "panel p-2 min-w-[140px]",
                                        children: [
                                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                className: "text-xs font-semibold text-gray-300 mb-1",
                                                children: [
                                                    "Corner ",
                                                    data.cornerNumber,
                                                    " (",
                                                    data.cornerType,
                                                    ")"
                                                ]
                                            }),
                                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                className: "text-xs text-gray-200",
                                                children: [
                                                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                        children: [
                                                            driver1,
                                                            ": ",
                                                            data.driver1Time?.toFixed(3),
                                                            "s"
                                                        ]
                                                    }),
                                                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                        children: [
                                                            driver2,
                                                            ": ",
                                                            data.driver2Time?.toFixed(3),
                                                            "s"
                                                        ]
                                                    }),
                                                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                        className: "mt-1 font-mono",
                                                        children: [
                                                            "Δ: ",
                                                            data.delta !== null ? (data.delta > 0 ? "+" : "") + data.delta.toFixed(3) : "N/A",
                                                            "s"
                                                        ]
                                                    })
                                                ]
                                            })
                                        ]
                                    });
                                }
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* ReferenceLine */.db, {
                                y: 0,
                                stroke: "#9aa4b2",
                                strokeDasharray: "2 2"
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx(lib/* Bar */.$Q, {
                                dataKey: "delta",
                                radius: [
                                    4,
                                    4,
                                    0,
                                    0
                                ],
                                children: deltaData.map((entry, index)=>/*#__PURE__*/ jsx_runtime_.jsx(lib/* Cell */.bL, {
                                        fill: entry.delta === null ? "#6b7280" : entry.delta > 0 ? "#ef4444" : "#22c55e"
                                    }, `cell-${index}`))
                            })
                        ]
                    })
                })
            })
        ]
    });
}

;// CONCATENATED MODULE: ./lib/sessionDataClient.ts
async function loadSessionData(request, init) {
    const { year, round, session, drivers } = request;
    const params = new URLSearchParams();
    if (drivers?.length) {
        params.set("drivers", drivers.join(","));
    }
    const url = `/api/sessions/${year}/${round}/${session}${params.toString() ? `?${params}` : ""}`;
    const res = await fetch(url, {
        cache: "no-cache",
        ...init
    });
    if (!res.ok) {
        throw new Error(`Failed to load session data (${res.status})`);
    }
    return res.json();
}

;// CONCATENATED MODULE: ./components/ClientPage.tsx
/* __next_internal_client_entry_do_not_use__ default auto */ 








function ClientPage() {
    const [selectedTrack, setSelectedTrack] = (0,react_.useState)("");
    const [trackData, setTrackData] = (0,react_.useState)(null);
    const [selectedDrivers, setSelectedDrivers] = (0,react_.useState)([
        "VER",
        "NOR"
    ]);
    const [selectedSession, setSelectedSession] = (0,react_.useState)("Q");
    const [sessionData, setSessionData] = (0,react_.useState)(null);
    const [sessionLoading, setSessionLoading] = (0,react_.useState)(false);
    const [sessionError, setSessionError] = (0,react_.useState)(null);
    const [showOutliers, setShowOutliers] = (0,react_.useState)(false);
    (0,react_.useEffect)(()=>{
        fetch("/data/tracks.json").then((r)=>r.json()).then(setTrackData);
    }, []);
    const [selectedYear, setSelectedYear] = (0,react_.useState)(new Date().getFullYear());
    const [availableYears, setAvailableYears] = (0,react_.useState)([]);
    const [availableRoundsByYear, setAvailableRoundsByYear] = (0,react_.useState)({});
    const [sessionsByRound, setSessionsByRound] = (0,react_.useState)({});
    const [calendarData, setCalendarData] = (0,react_.useState)(null);
    (0,react_.useEffect)(()=>{
        // Discover available sessions (years/rounds) via server-side index
        fetch("/api/sessions/index").then((r)=>r.json()).then((idx)=>{
            const years = Object.keys(idx?.years ?? {}).map(Number).sort((a, b)=>a - b);
            setAvailableYears(years);
            if (years.length) {
                const latest = years[years.length - 1];
                setSelectedYear(latest);
            }
            const map = {};
            const sessionsMap = {};
            for (const [year, payload] of Object.entries(idx?.years ?? {})){
                const rounds = payload?.rounds;
                map[year] = Array.isArray(rounds) ? rounds.map((r)=>r.id) : [];
                // Store sessions per round per year
                sessionsMap[year] = {};
                if (Array.isArray(rounds)) {
                    for (const round of rounds){
                        if (round.id && Array.isArray(round.sessions)) {
                            sessionsMap[year][round.id] = round.sessions;
                        }
                    }
                }
            }
            setAvailableRoundsByYear(map);
            setSessionsByRound(sessionsMap);
        }).catch(()=>{
        // leave empty on failure
        });
    }, []);
    (0,react_.useEffect)(()=>{
        // Load calendar data for the selected year to get round ordering
        fetch(`/data/calendar${selectedYear}.json`).then((r)=>r.json()).then(setCalendarData).catch(()=>{
            // If calendar file doesn't exist for this year, that's okay
            setCalendarData(null);
        });
    }, [
        selectedYear
    ]);
    (0,react_.useEffect)(()=>{
        if (!selectedTrack) {
            setSessionData(null);
            setSessionError(null);
            setSessionLoading(false);
            return;
        }
        const controller = new AbortController();
        setSessionLoading(true);
        setSessionError(null);
        loadSessionData({
            year: selectedYear,
            round: selectedTrack,
            session: selectedSession,
            drivers: selectedDrivers
        }, {
            signal: controller.signal
        }).then((data)=>{
            if (controller.signal.aborted) return;
            setSessionData(data);
            setSessionLoading(false);
        }).catch((error)=>{
            if (controller.signal.aborted) return;
            setSessionData(null);
            setSessionError(error instanceof Error ? error.message : String(error));
            setSessionLoading(false);
        });
        return ()=>controller.abort();
    }, [
        selectedTrack,
        selectedSession,
        selectedDrivers,
        selectedYear
    ]);
    const currentTrack = trackData?.tracks[selectedTrack];
    // Get available sessions for the selected track
    const availableSessions = (0,react_.useMemo)(()=>{
        if (!selectedTrack || !selectedYear) return [];
        const yearSessions = sessionsByRound[String(selectedYear)];
        if (!yearSessions) return [];
        return yearSessions[selectedTrack] ?? [];
    }, [
        selectedTrack,
        selectedYear,
        sessionsByRound
    ]);
    // Reset selected session if it's not available for the selected track
    (0,react_.useEffect)(()=>{
        if (selectedTrack && availableSessions.length > 0) {
            if (!availableSessions.includes(selectedSession)) {
                // Reset to first available session (prefer Q, then R, then first available)
                const preferredOrder = [
                    "Q",
                    "R",
                    "SQ",
                    "S"
                ];
                const preferred = preferredOrder.find((s)=>availableSessions.includes(s));
                const newSession = preferred || availableSessions[0];
                if (newSession !== selectedSession) {
                    setSelectedSession(newSession);
                }
            }
        }
    }, [
        selectedTrack,
        availableSessions,
        selectedSession
    ]);
    const sessionLabel = (0,react_.useMemo)(()=>{
        const found = sessionOptions.find((option)=>option.value === selectedSession);
        return found?.label ?? selectedSession;
    }, [
        selectedSession
    ]);
    const roundIds = availableRoundsByYear[String(selectedYear)] ?? [];
    // Create a map of round ID to round number from calendar data for sorting
    const roundNumberMap = (0,react_.useMemo)(()=>{
        if (!calendarData?.rounds) return new Map();
        const map = new Map();
        calendarData.rounds.forEach((round)=>{
            map.set(round.id, round.round);
        });
        return map;
    }, [
        calendarData
    ]);
    const trackList = (0,react_.useMemo)(()=>{
        if (!trackData) return [];
        const tracks = roundIds.filter((id)=>!!trackData.tracks[id]).map((id)=>{
            const trackInfo = trackData.tracks[id];
            return {
                id,
                name: trackInfo.name
            };
        });
        // Sort by round number if calendar data is available, otherwise keep original order
        if (roundNumberMap.size > 0) {
            tracks.sort((a, b)=>{
                const roundA = roundNumberMap.get(a.id) ?? 999;
                const roundB = roundNumberMap.get(b.id) ?? 999;
                return roundA - roundB;
            });
        }
        return tracks;
    }, [
        roundIds,
        trackData,
        roundNumberMap
    ]);
    if (!trackData || !availableYears.length) return /*#__PURE__*/ jsx_runtime_.jsx("div", {
        children: "Loading..."
    });
    return /*#__PURE__*/ (0,jsx_runtime_.jsxs)("main", {
        className: "max-w-6xl mx-auto px-4",
        children: [
            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("header", {
                className: "mb-10 flex flex-col items-center gap-4 text-center",
                children: [
                    /*#__PURE__*/ jsx_runtime_.jsx("div", {
                        className: "flex h-16 w-16 items-center justify-center overflow-hidden rounded-full border border-accent/40 bg-gray-900/40",
                        children: /*#__PURE__*/ jsx_runtime_.jsx((image_default()), {
                            src: "/logos/logo-transparent.png",
                            alt: "F1 Corner Analysis logo",
                            width: 64,
                            height: 64,
                            className: "object-contain",
                            priority: true
                        })
                    }),
                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx("h1", {
                                className: "text-4xl font-semibold tracking-tight text-white md:text-5xl",
                                children: "F1 Corner Analysis"
                            }),
                            /*#__PURE__*/ jsx_runtime_.jsx("p", {
                                className: "mt-2 text-sm text-subtext-clr md:text-base",
                                children: "with data from FastF1"
                            })
                        ]
                    })
                ]
            }),
            /*#__PURE__*/ jsx_runtime_.jsx(Toolbar, {
                tracks: trackList,
                selectedTrack: selectedTrack,
                onTrackChangeAction: setSelectedTrack,
                years: availableYears,
                selectedYear: selectedYear,
                onYearChangeAction: (y)=>{
                    setSelectedYear(y);
                    setSelectedTrack("");
                },
                selectedDrivers: selectedDrivers,
                onDriversChangeAction: setSelectedDrivers,
                selectedSession: selectedSession,
                onSessionChangeAction: setSelectedSession,
                availableSessions: availableSessions
            }),
            currentTrack && /*#__PURE__*/ (0,jsx_runtime_.jsxs)(jsx_runtime_.Fragment, {
                children: [
                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                        className: "mt-6 grid lg:grid-cols-2 gap-6",
                        children: [
                            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                className: "panel p-4",
                                children: /*#__PURE__*/ jsx_runtime_.jsx(TrackPanel, {
                                    svgFile: currentTrack.svgFile,
                                    corners: currentTrack.corners
                                })
                            }),
                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                className: "panel p-4",
                                children: [
                                    /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                        className: "text-lg font-bold",
                                        children: trackData.tracks[selectedTrack]?.name
                                    }),
                                    /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                        className: "text-gray-600",
                                        children: selectedYear
                                    }),
                                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                        className: "mt-2 text-sm text-gray-400",
                                        children: [
                                            "Session: ",
                                            sessionLabel
                                        ]
                                    }),
                                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                        className: "mt-4 text-sm text-gray-300",
                                        children: [
                                            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                                className: "font-semibold uppercase tracking-wide text-xs text-gray-400",
                                                children: "Selected Drivers"
                                            }),
                                            /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                                className: "mt-1 flex flex-wrap gap-2",
                                                children: selectedDrivers.length ? selectedDrivers.map((code)=>/*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                        className: "inline-flex items-center justify-center rounded-full border border-gray-700 bg-gray-800/70 px-2 py-0.5 text-xs font-medium text-gray-200",
                                                        children: code
                                                    }, code)) : /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                    className: "text-gray-500",
                                                    children: "No drivers selected"
                                                })
                                            })
                                        ]
                                    }),
                                    sessionData?.meta && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                        className: "mt-3 flex flex-wrap items-center gap-3 text-xs text-gray-500",
                                        children: [
                                            /*#__PURE__*/ (0,jsx_runtime_.jsxs)("span", {
                                                children: [
                                                    sessionData.meta.validLapCount ?? sessionData.meta.totalLapCount ?? 0,
                                                    " valid laps",
                                                    typeof sessionData.meta.totalLapCount === "number" && typeof sessionData.meta.validLapCount === "number" ? ` / ${sessionData.meta.totalLapCount}` : ""
                                                ]
                                            }),
                                            typeof sessionData.meta.outlierLapCount === "number" && sessionData.meta.outlierLapCount > 0 && /*#__PURE__*/ (0,jsx_runtime_.jsxs)("span", {
                                                className: "text-[11px] text-gray-400",
                                                children: [
                                                    sessionData.meta.outlierLapCount,
                                                    " flagged as outliers"
                                                ]
                                            })
                                        ]
                                    }),
                                    /*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                        className: "mt-3 flex items-center gap-2 text-xs text-gray-300",
                                        children: [
                                            /*#__PURE__*/ jsx_runtime_.jsx("button", {
                                                type: "button",
                                                onClick: ()=>setShowOutliers((prev)=>!prev),
                                                className: `rounded border px-2 py-1 transition ${showOutliers ? "border-accent text-accent" : "border-gray-600 text-gray-400 hover:border-accent/40 hover:text-accent"}`,
                                                children: showOutliers ? "Hide outlier laps" : "Show outlier laps"
                                            }),
                                            /*#__PURE__*/ jsx_runtime_.jsx("span", {
                                                className: "text-[11px] text-gray-500",
                                                children: "Outliers include out/in laps, safety car laps, yellow flag laps, etc."
                                            })
                                        ]
                                    }),
                                    sessionError && /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                        className: "mt-4 rounded border border-red-500/40 bg-red-500/10 px-3 py-2 text-sm text-red-300",
                                        children: sessionError
                                    }),
                                    sessionData?.notes?.length ? /*#__PURE__*/ jsx_runtime_.jsx("div", {
                                        className: "mt-4 text-xs text-gray-500 space-y-1",
                                        children: sessionData.notes.map((note, idx)=>/*#__PURE__*/ (0,jsx_runtime_.jsxs)("div", {
                                                children: [
                                                    "• ",
                                                    note
                                                ]
                                            }, idx))
                                    }) : null
                                ]
                            })
                        ]
                    }),
                    /*#__PURE__*/ jsx_runtime_.jsx(ChartPanel, {
                        sessionData: sessionData,
                        selectedDrivers: selectedDrivers,
                        loading: sessionLoading,
                        showOutliers: showOutliers
                    }),
                    /*#__PURE__*/ jsx_runtime_.jsx(CornerTable, {
                        corners: sessionData?.corners ?? {},
                        cornerInfo: currentTrack.corners,
                        selectedDrivers: selectedDrivers
                    }),
                    selectedDrivers.length >= 2 && /*#__PURE__*/ jsx_runtime_.jsx(CornerDeltaChart, {
                        corners: sessionData?.corners ?? {},
                        cornerInfo: currentTrack.corners,
                        selectedDrivers: selectedDrivers
                    })
                ]
            })
        ]
    });
}


/***/ }),

/***/ 75200:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ RootLayout),
/* harmony export */   metadata: () => (/* binding */ metadata)
/* harmony export */ });
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(56786);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _globals_css__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(92817);
/* harmony import */ var _globals_css__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_globals_css__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(46261);



const metadata = {
    title: "F1 Corner Analysis",
    description: "with data from FastF1",
    icons: {
        icon: "/logos/logo-navy.png",
        shortcut: "/logos/logo-navy.png",
        apple: "/logos/logo-navy.png"
    }
};
function RootLayout({ children }) {
    return /*#__PURE__*/ react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx("html", {
        lang: "en",
        children: /*#__PURE__*/ react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx("body", {
            children: /*#__PURE__*/ react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx("div", {
                className: "min-h-screen py-8",
                children: children
            })
        })
    });
}


/***/ }),

/***/ 68810:
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, {
  "default": () => (/* binding */ Page)
});

// EXTERNAL MODULE: external "next/dist/compiled/react/jsx-runtime"
var jsx_runtime_ = __webpack_require__(56786);
// EXTERNAL MODULE: ./node_modules/next/dist/build/webpack/loaders/next-flight-loader/module-proxy.js
var module_proxy = __webpack_require__(17814);
;// CONCATENATED MODULE: ./components/ClientPage.tsx

const proxy = (0,module_proxy.createProxy)(String.raw`/Users/jasoncharwin/Personal Code Projects/F1-Corner-Analysis/components/ClientPage.tsx`)

// Accessing the __esModule property and exporting $$typeof are required here.
// The __esModule getter forces the proxy target to create the default export
// and the $$typeof value is for rendering logic to determine if the module
// is a client boundary.
const { __esModule, $$typeof } = proxy;
const __default__ = proxy.default;


/* harmony default export */ const ClientPage = (__default__);
;// CONCATENATED MODULE: ./app/page.tsx


function Page() {
    return /*#__PURE__*/ jsx_runtime_.jsx(ClientPage, {});
}


/***/ }),

/***/ 92817:
/***/ (() => {



/***/ })

};
;

// load runtime
var __webpack_require__ = require("../webpack-runtime.js");
__webpack_require__.C(exports);
var __webpack_exec__ = (moduleId) => (__webpack_require__(__webpack_require__.s = moduleId))
var __webpack_exports__ = __webpack_require__.X(0, [399,703], () => (__webpack_exec__(25934)));
module.exports = __webpack_exports__;

})();